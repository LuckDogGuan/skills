# [Osmosis比赛]  一键备份/复原你的Osmosis分数分配代码优化

- **链接**: https://support.worldquantbrain.com[Commented] [Osmosis比赛]  一键备份复原你的Osmosis分数分配代码优化_37271778231063.md
- **作者**: 顾问 RM49262 (Rank 30)
- **发布时间/热度**: 6个月前, 得票: 2

## 帖子正文

今天Osmosis比赛已经更新了Combo IR rank情况

根据规则 **截止北京时间下周一中午12:59，我们可以进一步更改方案重新分配点数，从而重新计算我们的分数，相当于还有机会尝试。**

论坛里目前已经有多种一键分配分数的参考方案贴，大家可以自行选择。

但是，分配分数之前，大家不要忘了备份本周我们的分数分配结果，这样一旦下周分配方案不理想，还可以轻松一键复原。

我这里参考了贺六浑一键清空分数的的帖子。

下面一共有两个脚本，第一个是一键备份，会生成一个json文件，来储存你的分数分配数据。

第二个脚本则是一键清空+复原，会根据上述json文件，来复原你的分数分配情况。大家可以根据自己的需求，按需取用。

两个脚本中，需要大家自行输入的只有 **用户名/密码/成为顾问的日期**

**如果好用的话，请大家顺便顺便点个赞！**

脚本一：一键备份

```
# -*- coding: utf-8 -*-"""Osmosis Points 备份工具备份 WorldQuant BRAIN 平台上各 Region 的 Alpha osmosisPoints 分配情况"""import jsonimport timeimport osfrom datetime import datetime, timedeltafrom collections import defaultdictfrom urllib.parse import urljoinimport requestsfrom requests.adapters import HTTPAdapterfrom urllib3.util.retry import Retry# ===================================================================# 用户配置区 - 请在此处填写您的信息# ===================================================================# WorldQuant BRAIN 账号EMAIL = ""           # 请填写您的邮箱PASSWORD = ""        # 请填写您的密码# 成为顾问的日期（Alpha 开始计算收益的日期）TOBE_CONSULTANT_DAY = "2025-08-14"# ===================================================================# Session 管理器（简化版）# ===================================================================class BrainSession:    """WorldQuant BRAIN 平台 Session 管理器"""    API_URL = "https://api.worldquantbrain.com"    def __init__(self, email: str, password: str):        self._email = email        self._password = password        self._session = None    def _setup_retry_adapter(self):        """配置自动重试策略"""        retry_strategy = Retry(            total=3,            backoff_factor=1,            status_forcelist=[429, 500, 502, 503, 504],        )        adapter = HTTPAdapter(            max_retries=retry_strategy,            pool_connections=20,            pool_maxsize=20,        )        self._session.mount("http://", adapter)        self._session.mount("https://", adapter)    def login(self):        """登录 BRAIN 平台"""        self._session = requests.Session()        self._session.auth = (self._email, self._password)        self._setup_retry_adapter()        max_retries = 5        for attempt in range(max_retries):            try:                response = self._session.post(f"{self.API_URL}/authentication")                if response.status_code == 401:                    if response.headers.get("WWW-Authenticate") == "persona":                        # 需要生物识别认证                        biometric_url = urljoin(response.url, response.headers["Location"])                        print(f"\n请在浏览器中完成生物识别认证后按 Enter 继续:\n{biometric_url}\n")                        input()                        for i in range(5):                            bio_response = self._session.post(biometric_url)                            if bio_response.status_code == 201:                                print("生物识别认证成功")                                return self._session                            print(f"认证未完成，请重试并按 Enter...")                            input()                        raise RuntimeError("生物识别认证失败")                    else:                        raise ValueError("邮箱或密码错误，请检查凭证")                elif response.status_code // 100 == 2:                    print("登录成功")                    return self._session                else:                    print(f"登录尝试 {attempt + 1} 失败: {response.status_code}")                    if attempt < max_retries - 1:                        time.sleep(10)                        continue                    raise RuntimeError(f"登录失败: {response.status_code}")            except requests.RequestException as e:                print(f"登录请求失败 (尝试 {attempt + 1}/{max_retries}): {e}")                if attempt < max_retries - 1:                    time.sleep(10)                    continue                raise        raise RuntimeError("登录失败：已达到最大重试次数")    def get(self, url: str, **kwargs) -> requests.Response:        """发送 GET 请求"""        if not url.startswith("http"):            url = f"{self.API_URL}{url}" if url.startswith("/") else f"{self.API_URL}/{url}"        for attempt in range(3):            try:                response = self._session.get(url, timeout=(15, 60), **kwargs)                if response.status_code == 401:                    print("收到 401，重新登录...")                    self.login()                    continue                return response            except requests.RequestException as e:                print(f"GET 请求失败 (尝试 {attempt + 1}/3): {e}")                if attempt < 2:                    time.sleep(5 * (attempt + 1))                else:                    raise        return response# ===================================================================# 备份功能# ===================================================================def get_colored_alphas_in_date_range(session, start_date, end_date, alpha_num_limit=1000):    """    获取在指定日期范围内，所有设置了分数的常规 (non-SUPER) Alpha。    """    colored_alphas = []    print(f"开始查找从 {start_date} 到 {end_date} 所有已设置分数的常规 Alpha...")    for i in range(0, alpha_num_limit, 100):        print(f"正在扫描第 {i} 到 {i + 100} 个 alpha...")        url = (            f"/users/self/alphas?limit=100&offset={i}"            f"&status!=UNSUBMITTED&status!=IS_FAIL&type!=SUPER&hidden=false"            f"&dateSubmitted>={start_date}T00:00:00-04:00"            f"&dateSubmitted<{end_date}T00:00:00-04:00"        )        try:            response = session.get(url)            response.raise_for_status()            alpha_list = response.json().get("results", [])            if not alpha_list:                print("已扫描完所有符合条件的 Alpha。")                break            # 筛选出有分数的 Alpha，并记录 region 信息            for alpha in alpha_list:                if alpha.get("osmosisPoints") is not None:                    settings = alpha.get("settings", {})                    record = {                        "alpha_id": alpha["id"],                        "osmosisPoints": alpha["osmosisPoints"],                        "region": settings.get("region", "UNKNOWN")                    }                    colored_alphas.append(record)        except Exception as e:            print(f"获取 alpha 时发生异常: {e}")            break    print(f"\n查找完毕！共找到 {len(colored_alphas)} 个已设置分数的 Alpha。")    return colored_alphasdef group_by_region(alphas):    """将 alpha 列表按 region 分组"""    grouped = defaultdict(list)    for alpha in alphas:        region = alpha["region"]        grouped[region].append({            "alpha_id": alpha["alpha_id"],            "osmosisPoints": alpha["osmosisPoints"]        })    return dict(grouped)def save_backup_to_json(backup_data, output_path):    """将备份数据保存为 JSON 文件"""    with open(output_path, 'w', encoding='utf-8') as f:        json.dump(backup_data, f, ensure_ascii=False, indent=2)    print(f"备份已保存至: {output_path}")# ===================================================================# 主程序# ===================================================================if __name__ == "__main__":    # 检查用户配置    if not EMAIL or not PASSWORD:        print("错误：请先在脚本开头填写 EMAIL 和 PASSWORD")        exit(1)    # 登录    session = BrainSession(EMAIL, PASSWORD)    session.login()    # 计算日期范围    begin_date = TOBE_CONSULTANT_DAY    end_date_obj = datetime.now() + timedelta(days=1)    end_date = end_date_obj.strftime("%Y-%m-%d")    print("-" * 50)    print("本脚本将查找并备份指定日期范围内的常规 Alpha 分数。")    print(f"顾问开始日 (脚本起始日期): {begin_date}")    print(f"脚本截止日期: {end_date}")    print("-" * 50)    # 获取指定日期范围内带分数的 Alphas    alphas_with_points = get_colored_alphas_in_date_range(session, begin_date, end_date)    if not alphas_with_points:        print("在指定日期范围内未找到任何设置了分数的 Alpha，程序结束。")    else:        # 按 Region 分组        grouped_data = group_by_region(alphas_with_points)        # 构建备份数据结构        backup_time = datetime.now().strftime("%Y-%m-%d %H:%M:%S")        backup_data = {            "backup_time": backup_time,            "date_range": {                "start": begin_date,                "end": end_date            },            "total_alphas": len(alphas_with_points),            "regions": grouped_data        }        # 保存为 JSON 文件（脚本所在目录）        script_dir = os.path.dirname(os.path.abspath(__file__))        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")        output_filename = os.path.join(script_dir, f"osmosis_points_backup_{timestamp}.json")        save_backup_to_json(backup_data, output_filename)        # 打印总结报告        print("\n" + "=" * 50)        print("备份完成！")        print(f"\n总结报告:")        print(f"- 备份时间: {backup_time}")        print(f"- 总 Alpha 数量: {len(alphas_with_points)}")        print(f"- Region 分布:")        for region, alphas in grouped_data.items():            total_points = sum(a["osmosisPoints"] for a in alphas)            print(f"  - {region}: {len(alphas)} 个 Alpha, 共 {total_points} 点")        print(f"\n备份文件: {output_filename}")        print("=" * 50)
```

**脚本二：一键清空分数+复原备份分配**

```
# -*- coding: utf-8 -*-"""Osmosis Points 恢复工具根据备份的 JSON 文件恢复 Alpha 的 osmosisPoints 分配流程：1. 清空所有现有分数 2. 根据备份文件重新分配"""import jsonimport timeimport osfrom datetime import datetime, timedeltafrom concurrent.futures import ThreadPoolExecutorfrom urllib.parse import urljoinimport requestsfrom requests.adapters import HTTPAdapterfrom urllib3.util.retry import Retry# ===================================================================# 用户配置区 - 请在此处填写您的信息# ===================================================================# WorldQuant BRAIN 账号EMAIL = ""           # 请填写您的邮箱PASSWORD = ""        # 请填写您的密码# 成为顾问的日期（Alpha 开始计算收益的日期）TOBE_CONSULTANT_DAY = "2025-08-14"# 要恢复的 JSON 备份文件路径（留空则使用最新的备份文件）BACKUP_JSON_PATH = ""# 并发操作的线程数MAX_WORKERS = 10# ===================================================================# Session 管理器（简化版）# ===================================================================class BrainSession:    """WorldQuant BRAIN 平台 Session 管理器"""    API_URL = "https://api.worldquantbrain.com"    def __init__(self, email: str, password: str):        self._email = email        self._password = password        self._session = None    def _setup_retry_adapter(self):        """配置自动重试策略"""        retry_strategy = Retry(            total=3,            backoff_factor=1,            status_forcelist=[429, 500, 502, 503, 504],        )        adapter = HTTPAdapter(            max_retries=retry_strategy,            pool_connections=20,            pool_maxsize=20,        )        self._session.mount("http://", adapter)        self._session.mount("https://", adapter)    def login(self):        """登录 BRAIN 平台"""        self._session = requests.Session()        self._session.auth = (self._email, self._password)        self._setup_retry_adapter()        max_retries = 5        for attempt in range(max_retries):            try:                response = self._session.post(f"{self.API_URL}/authentication")                if response.status_code == 401:                    if response.headers.get("WWW-Authenticate") == "persona":                        # 需要生物识别认证                        biometric_url = urljoin(response.url, response.headers["Location"])                        print(f"\n请在浏览器中完成生物识别认证后按 Enter 继续:\n{biometric_url}\n")                        input()                        for i in range(5):                            bio_response = self._session.post(biometric_url)                            if bio_response.status_code == 201:                                print("生物识别认证成功")                                return self._session                            print(f"认证未完成，请重试并按 Enter...")                            input()                        raise RuntimeError("生物识别认证失败")                    else:                        raise ValueError("邮箱或密码错误，请检查凭证")                elif response.status_code // 100 == 2:                    print("登录成功")                    return self._session                else:                    print(f"登录尝试 {attempt + 1} 失败: {response.status_code}")                    if attempt < max_retries - 1:                        time.sleep(10)                        continue                    raise RuntimeError(f"登录失败: {response.status_code}")            except requests.RequestException as e:                print(f"登录请求失败 (尝试 {attempt + 1}/{max_retries}): {e}")                if attempt < max_retries - 1:                    time.sleep(10)                    continue                raise        raise RuntimeError("登录失败：已达到最大重试次数")    def get(self, url: str, **kwargs) -> requests.Response:        """发送 GET 请求"""        if not url.startswith("http"):            url = f"{self.API_URL}{url}" if url.startswith("/") else f"{self.API_URL}/{url}"        for attempt in range(3):            try:                response = self._session.get(url, timeout=(15, 60), **kwargs)                if response.status_code == 401:                    print("收到 401，重新登录...")                    self.login()                    continue                return response            except requests.RequestException as e:                print(f"GET 请求失败 (尝试 {attempt + 1}/3): {e}")                if attempt < 2:                    time.sleep(5 * (attempt + 1))                else:                    raise        return response    def patch(self, url: str, **kwargs) -> requests.Response:        """发送 PATCH 请求"""        if not url.startswith("http"):            url = f"{self.API_URL}{url}" if url.startswith("/") else f"{self.API_URL}/{url}"        for attempt in range(3):            try:                response = self._session.patch(url, timeout=(15, 60), **kwargs)                if response.status_code == 401:                    print("收到 401，重新登录...")                    self.login()                    continue                return response            except requests.RequestException as e:                print(f"PATCH 请求失败 (尝试 {attempt + 1}/3): {e}")                if attempt < 2:                    time.sleep(5 * (attempt + 1))                else:                    raise        return response# ===================================================================# 清空分数功能# ===================================================================def get_colored_alphas_in_date_range(session, start_date, end_date, alpha_num_limit=1000):    """    获取在指定日期范围内，所有设置了分数的常规 (non-SUPER) Alpha。    """    colored_alphas = []    print(f"开始查找从 {start_date} 到 {end_date} 所有已设置分数的常规 Alpha...")    for i in range(0, alpha_num_limit, 100):        print(f"正在扫描第 {i} 到 {i + 100} 个 alpha...")        url = (            f"/users/self/alphas?limit=100&offset={i}"            f"&status!=UNSUBMITTED&status!=IS_FAIL&type!=SUPER&hidden=false"            f"&dateSubmitted>={start_date}T00:00:00-04:00"            f"&dateSubmitted<{end_date}T00:00:00-04:00"        )        try:            response = session.get(url)            response.raise_for_status()            alpha_list = response.json().get("results", [])            if not alpha_list:                print("已扫描完所有符合条件的 Alpha。")                break            # 筛选出有分数的 Alpha            for alpha in alpha_list:                if alpha.get("osmosisPoints") is not None:                    record = {                        "id": alpha["id"],                        "osmosisPoints": alpha["osmosisPoints"]                    }                    colored_alphas.append(record)        except Exception as e:            print(f"获取 alpha 时发生异常: {e}")            break    print(f"\n查找完毕！共找到 {len(colored_alphas)} 个已设置分数的 Alpha。")    return colored_alphasdef clear_alpha_points(session, alpha_id: str, old_points):    """    清除单个 Alpha 的分数（设置为 null）    """    response = session.patch(        f"/alphas/{alpha_id}",        json={"osmosisPoints": None}    )    if response.status_code == 200:        print(f"  清除成功: Alpha {alpha_id} (原分数: {old_points})")        return "SUCCESS"    else:        print(f"  清除失败: Alpha {alpha_id}, 状态码: {response.status_code}")        return "FAILED"def clear_all_points(session, start_date, end_date):    """    清空所有分数    """    print("\n" + "=" * 50)    print("阶段 1: 清空现有分数")    print("=" * 50)    # 获取所有有分数的 Alpha    alphas_to_clear = get_colored_alphas_in_date_range(session, start_date, end_date)    if not alphas_to_clear:        print("没有找到需要清除分数的 Alpha。")        return 0    print(f"\n开始清除 {len(alphas_to_clear)} 个 Alpha 的分数...")    # 并发清除    success_count = 0    failed_count = 0    with ThreadPoolExecutor(max_workers=MAX_WORKERS) as executor:        tasks = []        for alpha_data in alphas_to_clear:            task = executor.submit(                clear_alpha_points,                session,                alpha_data["id"],                alpha_data["osmosisPoints"]            )            tasks.append(task)        for task in tasks:            result = task.result()            if result == "SUCCESS":                success_count += 1            else:                failed_count += 1    print(f"\n清除完成: 成功 {success_count} 个, 失败 {failed_count} 个")    return success_count# ===================================================================# 恢复分数功能# ===================================================================def find_latest_backup_file(script_dir):    """    查找脚本所在目录中最新的备份文件    """    backup_files = []    for filename in os.listdir(script_dir):        if filename.startswith("osmosis_points_backup_") and filename.endswith(".json"):            filepath = os.path.join(script_dir, filename)            backup_files.append((filepath, os.path.getmtime(filepath)))    if not backup_files:        return None    # 按修改时间排序，返回最新的    backup_files.sort(key=lambda x: x[1], reverse=True)    return backup_files[0][0]def load_backup_data(json_path):    """    加载备份 JSON 文件    """    with open(json_path, 'r', encoding='utf-8') as f:        return json.load(f)def restore_alpha_points(session, alpha_id: str, points: int):    """    恢复单个 Alpha 的分数    """    response = session.patch(        f"/alphas/{alpha_id}",        json={"osmosisPoints": points}    )    if response.status_code == 200:        print(f"  恢复成功: Alpha {alpha_id} -> {points} 点")        return "SUCCESS"    else:        print(f"  恢复失败: Alpha {alpha_id}, 状态码: {response.status_code}")        return "FAILED"def restore_points_from_backup(session, backup_data):    """    根据备份数据恢复分数    """    print("\n" + "=" * 50)    print("阶段 2: 恢复分数")    print("=" * 50)    regions = backup_data.get("regions", {})    total_alphas = backup_data.get("total_alphas", 0)    print(f"备份时间: {backup_data.get('backup_time', 'N/A')}")    print(f"备份日期范围: {backup_data.get('date_range', {}).get('start', 'N/A')} 到 {backup_data.get('date_range', {}).get('end', 'N/A')}")    print(f"总 Alpha 数量: {total_alphas}")    print(f"Region 数量: {len(regions)}")    # 收集所有需要恢复的 Alpha    all_alphas = []    for region, alphas in regions.items():        for alpha in alphas:            all_alphas.append({                "alpha_id": alpha["alpha_id"],                "osmosisPoints": alpha["osmosisPoints"],                "region": region            })    print(f"\n开始恢复 {len(all_alphas)} 个 Alpha 的分数...")    # 并发恢复    success_count = 0    failed_count = 0    region_stats = {}    with ThreadPoolExecutor(max_workers=MAX_WORKERS) as executor:        tasks = []        for alpha_data in all_alphas:            task = executor.submit(                restore_alpha_points,                session,                alpha_data["alpha_id"],                alpha_data["osmosisPoints"]            )            tasks.append((task, alpha_data))        for task, alpha_data in tasks:            result = task.result()            region = alpha_data["region"]            if region not in region_stats:                region_stats[region] = {"success": 0, "failed": 0, "points": 0}            if result == "SUCCESS":                success_count += 1                region_stats[region]["success"] += 1                region_stats[region]["points"] += alpha_data["osmosisPoints"]            else:                failed_count += 1                region_stats[region]["failed"] += 1    return success_count, failed_count, region_stats# ===================================================================# 主程序# ===================================================================if __name__ == "__main__":    # 检查用户配置    if not EMAIL or not PASSWORD:        print("错误：请先在脚本开头填写 EMAIL 和 PASSWORD")        exit(1)    # 获取脚本所在目录    script_dir = os.path.dirname(os.path.abspath(__file__))    # 确定备份文件路径    if BACKUP_JSON_PATH:        backup_path = BACKUP_JSON_PATH    else:        backup_path = find_latest_backup_file(script_dir)    if not backup_path or not os.path.exists(backup_path):        print("错误：未找到备份文件！")        print("请在 BACKUP_JSON_PATH 中指定备份文件路径，或确保脚本目录中有 osmosis_points_backup_*.json 文件")        exit(1)    print("=" * 60)    print("Osmosis Points 恢复工具")    print("=" * 60)    print(f"备份文件: {backup_path}")    # 加载备份数据    backup_data = load_backup_data(backup_path)    print(f"备份时间: {backup_data.get('backup_time', 'N/A')}")    print(f"备份中的 Alpha 数量: {backup_data.get('total_alphas', 0)}")    # 确认操作    print("\n" + "-" * 60)    print("警告：此操作将先清空所有现有分数，然后根据备份文件重新分配！")    print("-" * 60)    confirm = input("是否继续？(输入 'yes' 确认): ")    if confirm.lower() != 'yes':        print("操作已取消。")        exit(0)    # 登录    session = BrainSession(EMAIL, PASSWORD)    session.login()    # 计算日期范围    begin_date = TOBE_CONSULTANT_DAY    end_date_obj = datetime.now() + timedelta(days=1)    end_date = end_date_obj.strftime("%Y-%m-%d")    # 阶段 1: 清空现有分数    cleared_count = clear_all_points(session, begin_date, end_date)    # 阶段 2: 恢复分数    success_count, failed_count, region_stats = restore_points_from_backup(session, backup_data)    # 打印总结报告    print("\n" + "=" * 60)    print("恢复完成！")    print("=" * 60)    print(f"\n总结报告:")    print(f"- 清空的 Alpha 数量: {cleared_count}")    print(f"- 恢复成功: {success_count} 个")    print(f"- 恢复失败: {failed_count} 个")    print(f"\nRegion 分布:")    for region, stats in region_stats.items():        print(f"  - {region}: 成功 {stats['success']} 个, 共 {stats['points']} 点")    print("=" * 60)
```

---

## 讨论与评论 (2)

### 评论 #1 (作者: FF65210, 时间: 2个月前)

感谢佬的代码，可以测试自己不同的os分配来观察哪个更好了，非常好用。

**===============================================================================
守正心，戒浮躁，恒复盘，危中机。“复利是世界第八大奇迹，关键在于找到可长期重复的正确策略，日复一日、不疾不徐地执行。”—— 爱因斯坦 / 查理・芒格
Alpha∞ Engine Status: ONLINE [♦♦♦♦♦♦♦♦♦♦] 100%
sys.setrecursionlimit(α∞)
PnL = ∑(Robustness * Creativity)
#无限探索、鲁棒性优先，创新性增值# 有志者事竟成，苦心人天不负。千淘万漉虽辛苦，吹尽狂沙始到金。
=================================================================================**

---

### 评论 #2 (作者: 顾问 MZ45384 (大角羊) (Rank 51), 时间: 1个月前)

这真是太有用了，最近os一直低迷，我也懒得去记录哪些策略有效了。有了这个代码，就可以保存以前的方案了。

======================================================================================
知难上，戒骄狂，常自省，穷途明。“寻找可以重复数千次的东西。”——吉姆·西蒙斯（量化投资之王、文艺复兴科技创始人）
# Alpha∞ Engine Status: ONLINE [♦♦♦♦♦♦♦♦♦♦] 100%
# sys.setrecursionlimit(α∞) 
# PnL = ∑(Robustness * Creativity)
#无限探索、鲁棒性优先，创新性增值 
#Where there is a will, there is a way. 路漫漫其修远兮，吾将上下而求索。
======================================================================================

---

