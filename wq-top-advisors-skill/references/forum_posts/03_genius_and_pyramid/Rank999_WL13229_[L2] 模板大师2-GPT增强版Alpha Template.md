# 模板大师2-GPT增强版Alpha Template

- **链接**: [L2] 模板大师2-GPT增强版Alpha Template.md
- **作者**: WL13229
- **发布时间/热度**: 1 year ago, 得票: 26

## 帖子正文

```
[该内容由社区成员共建,对本论坛中《Alpha灵感》系列文章进行阅读并提取模板，同时使用大语言模型进行优化。使用前请认真自行考察其合理性与科学性    {        "idea": "布林带策略被扩展用于构建估值偏离因子。通过使用252个交易日内的市盈率收益（EP）的均值和标准差，构造估值中枢和带宽。如果某只股票的当前EP位于上轨或下轨之外，则该股票的估值被认为处于异常区间，如果基本面保持不变，EP很可能会回归正常区间。",        "人工修改后的template": "t = {i};\na = ts_zscore({j}, t);\na1 = group_neutralize(a, bucket(rank(cap), range=\"0.1,1,0.1\"));\na2 = group_neutralize(a1, subindustry);\nb = ts_zscore(cap, t);\nb1 = group_neutralize(b, subindustry);\nregression_neut(a2, b1)",        "GPT输出的template": "\"mean_EP = ts_mean(EP, 252);\\nstd_EP = ts_std_dev(EP, 252);\\nupper_band = mean_EP + 2 * std_EP;\\nlower_band = mean_EP - 2 * std_EP;\\nsignal = (EP > upper_band) ? -1 : (EP < lower_band) ? 1 : 0;"    },    {        "idea": "该策略基于信息离散度（ID）来衡量股票价格波动的特性，使用变异系数（CV）作为ID的代理变量，结合动量缺口来改进动量反转因子。认为股票价格波动越连续（CV值越小），投资者反应不足，未来可能发生反转。同时考虑动量缺口，选择在动量缺口不高的情况下实施动量策略，以提高策略表现。",        "人工修改后的template": "CV = ts_std_dev((close/open - 1), 20) / ts_mean((close/open - 1), 20); internal = ts_delay(ts_percentage(returns, 20, percentage=0.9) - ts_percentage(returns, 20, percentage=0.1), 40); alpha = -RELEVANT_DATA_FIELD * rank(internal) * (1 / CV); alpha = group_neutralize(alpha, bucket(rank(cap), range=\"0.1,1,0.1\"))",        "GPT输出的template": "CV = ts_std_dev((close/open - 1), 20) / ts_mean((close/open - 1), 20); momentum = ts_rank(ts_sum(returns, 60), group); gap = ts_rank(ts_std_dev(returns, 60), group); alpha = trade_when(gap < threshold, -1, 1) * rank(1 / CV); alpha = group_neutralize(alpha, industry); alpha = ts_backfill(alpha, 20, 'nan');"    },    {        "idea": "该策略基于期权隐含价格与实际股价的差异（IPD），同时考虑期权的活跃度，通过未平仓头寸对价格差异进行调整，构建投资组合。论文指出这种策略在市值较小和难以做空的股票中效果显著。为解决数据覆盖率问题和减少权重集中，使用时间序列和横截面排名以及回填方法进行改进。",        "人工修改后的template": "group_rank(ts_backfill((ts_rank(IMPLIED_PRICE - close, d1)), 20),GROUP)",        "GPT输出的template": "IPD = ts_backfill((implied_price - actual_price), d); alpha = group_rank(IPD, industry); alpha = group_neutralize(alpha, industry)"    },    {        "idea": "研究指出应计项目与经营性现金流量（OCF）的相关性受多种经济和审计报告因素的影响。利用应计比率作为盈利质量的指标，较高的比率暗示较低的盈利质量和潜在的低回报。该策略基于此关系，通过行业中性化和时间序列分析，寻找具有较强应计异象的股票。",        "人工修改后的template": "accruals = income_beforeextra - cashflow_op - cashflow_invst - cashflow_fin;\n\naccrual_ratio = accruals/ts_mean(PV_DATA, d1);\n\ngroup_neutralize(\n    ts_rank(accrual_ratio , d2),\n    bucket(rank(cap),  range=\"0.1,1,0.1\")\n)\n",        "GPT输出的template": "accruals_ratio = accruals / ts_mean(price, 252); alpha = rank(accruals_ratio); alpha = group_neutralize(alpha, industry); alpha = ts_backfill(alpha, 20, 'nan')"    },    {        "idea": "论文指出，分析师长期与短期建议的不一致性会引起市场较大反应，而短期分析表现良好的分析师建议对股市有显著影响。策略利用短期建议变化（ts_av_diff）和分析师评级与回报关系的线性回归系数（ts_regression），结合分析师评级的排名（rank）来预测股票表现，并进行行业中性化处理。",        "人工修改后的template": "accuracy = rank(ts_regression(rank(returns), rank(ANALYST_ADVISE), d1, lag=0, rettype=2));\n\naccuracy > C ? 1 - rank(ts_av_diff(rating, d1)) : NaN",        "GPT输出的template": "short_term_recommendations = ts_av_diff(analyst_recommendations, 20, '1d'); long_term_recommendations = ts_mean(analyst_recommendations, 120, '1d'); recommendation_change = short_term_recommendations - ts_backfill(long_term_recommendations, 20, 'nan'); regression_coefficient = ts_regression(returns, recommendation_change, 120); alpha = ts_rank(regression_coefficient, 20) * (-1)^ts_rank(recommendation_change, 20); alpha = group_neutralize(alpha, industry)"    },    {        "idea": "使用过去20日股票实现波动率的标准差刻画股票在过去20个交易日的 “波动率的波动”，是对传统的波动率alpha的增强。如果一个股票在过去20天内波动率十分聚集，那么该指标的值就会变得更大。由于不同股票的波动率天然不同，因此除以过去20个交易日波动率均值进行标准化，消除量纲差异（即计算出变异系数），最后对波动率变异系数进行市值中性化。与传统波动率alpha一样，这个alpha提供的是负向信号，即做空值大的，做多值小的。",        "人工修改后的template": "std_dev = ts_std_dev(x, 20); mean = ts_mean(x, 20); cv = std_dev / mean; neut = regression_neut(cv, cap)",        "GPT输出的template": "std_dev = ts_std_dev(x, 20); mean = ts_mean(x, 20); cv = std_dev / mean; neut = rank(vector_neut(cv, cap))"    },    {        "idea": "通过观察高波动率股票的超额收益，发现短期内高波动股票表现优于市场，而在更长周期中则表现出强烈的反转效应。通过日内风险水平调整每日收益，提出一个统一的动量和反转因子UMR，捕捉高风险下的反转趋势与低风险下的动量效应。",        "人工修改后的template": "R1 = {i};\nt = {t};\nrisk = ts_mean(R1, t) - R1;\nmkt_ret = {j} / ts_delay({j}, 1) - 1;\ne = regression_neut(returns,mkt_ret);\nalpha = ts_sum(risk * e, t);\ngroup_rank(\n    alpha,\n    bucket(rank(cap), range='0.1,1,0.1')\n)",        "GPT输出的template": "t = {d};\nRisk_t = ts_sum(R, t) / t - R;\nrank_risk = ts_rank(Risk_t, t);\nhigh_risk_days = rank_risk > 0.8;\nlow_risk_days = rank_risk < 0.2;\nmomentum_low = ts_delta(R, t);\nreversal_high = -ts_delta(R, t);\nweight_momentum_low = ts_rank(momentum_low, t);\nweight_reversal_high = ts_rank(reversal_high, t);\nsignal = add(weight_momentum_low, weight_reversal_high, filter=true);signal_normalized = ts_scale(signal, t, constant=0.1);\nsignal_decayed = ts_decay_exp_window(signal_normalized, t, factor=0.5);\nAlpha = scale(signal_decayed, scale=1)"    },    {        "idea": "研究表明，期权的交易量(O)与股票成交量(S)的比值（O/S），对个股的未来收益存在着负向预测作用，并且这个负向预测效应会被隐含波动率所增强。使用行业的均值对数据字段缺失值进行填充，然后使用填充缺失值后的隐含波动率乘以填充缺失值后的O/S构建的alpha，可以显著的反向预测股票的收益率。",        "人工修改后的template": "group_extra(implied_volatility_call_1000 - implied_volatility_put_1000,1,industry)*group_extra(opt4_secpr_volume/volume),1,industry)",        "GPT输出的template": "filled_O_S = group_extra(O_S, industry);filled_iv = group_extra(iv, industry);product = filled_O_S * filled_iv;neutralized_product = group_neutralize(product, industry);signal = group_rank(-neutralized_product, industry)"    },    {        "idea": "股票价格的动量 (Momentum，顾名思义代表的是股价在一定时间内延续前期走势的现象。不过与海外长期的研究和经验相悖的是，在 A 股市场，我们发现股价的反转 (Reverse) 效应要远强于动量效应，且短期反转因子的历史收益非常出色。Momentum=returns*turnover，将换手率和收益率，以乘积的形式，结合起来表达市场的一种动量（力量）。如果某只股票在过去一段时间的动量，明显高于或低于同类型股票的平均水平，从概率上来讲，它的股价在未来可能会有所回归。（reversal strategy）。",        "人工修改后的template": "-ts_decay_exp_window(returns*{DAILY TURNOVER}, {N}, factor=0.04)",        "GPT输出的template": "\"-ts_zscore(returns * daily_turnover, {N})\""    },    {        "idea": "对11种常见技术指标进行尝试：每日真实范围（TR）；TR的移动平均（ATR）；移动平均收敛散度 (MACD) ；随机动量指标（SMI）；Aroon；平均趋向指数（ADX）；布林线 (Bollinger Bands)；Chaikin波动率；价格变动易度（EMV）；蜡烛线（Canddlestick）；资金流量指数 (MFI)\n\n\n这里看到另一篇帖子写template更详细（credict to TN48752 and TK95999)，涉及了20个左右的指标，大部分具有可深入讨论的价值，所以我在下面整理一下，扩大我的训练集。\n",        "人工修改后的template": "\\",        "GPT输出的template": "\\"    },    {        "idea": "RSI - Relative Strength Index\nThe relative strength index (RSI) is a momentum indicator used in technical analysis. RSI measures the speed and magnitude of a security's recent price changes to detect overvalued or undervalued conditions in the price of that security.",        "人工修改后的template": "window = 20;\noffset = 0.02;\ndelta = ts_delta({DAILY PRICE}, 1);\nRS = ts_sum((delta>0? delta: 0), window) / ts_sum((delta<0? delta: 0), window)+offset;\nRSI = 100 - 100/RS;\n-hump_decay(-RSI, p=4, relative=False)",        "GPT输出的template": ""    },    {        "idea": "Bollinger Bands is a technical analysis tool used to determine where prices are high and low relative to each other.\nThese bands are composed of three lines: a simple moving average (the middle band) and an upper and lower band.\nThe upper and lower bands are typically two standard deviations above or below a 20-period simple moving average (SMA).\nThe bands widen and narrow as the volatility of the underlying asset changes.",        "人工修改后的template": "window = 20;\nm = 2;\ntp =({PRICE1}+{PRICE2}+{PRICE3})/3;\nMA= ts_mean(tp,window);\nBOLU = MA + m * ts_std_dev(tp,window);\nBOLD = MA - m * ts_std_dev(tp,window);\nsignal = ts_sum(close<BOLD,window)-ts_sum(close>BOLU,window); \nts_zscore(signal,240)",        "GPT输出的template": ""    },    {        "idea": "MACD (Moving Average Convergence/Divergence)\n计算的是移动平均散度，即用过去12天的指数移动平均减去过去26天的指数移动平均",        "人工修改后的template": "EMA12 = ts_decay_exp_window({PRICE},12);\nEMA26 = ts_decay_exp_window({PRICE},26);\nMACD = EMA12 - EMA26",        "GPT输出的template": ""    },    {        "idea": "Average true range (ATR) does not provide an indication of price trend, simply the degree of price volatility. The average true range is an N-period smoothed moving average (SMMA) of the true range values. Wilder recommended a 14-period smoothing.",        "人工修改后的template": "window = 10;\nTR = max({HIGH PRICE}-{LOW PRICE},abs({HIGH PRICE}-ts_delay(close,1)),abs({LOW PRICE}-ts_delay(close,1)));\nATR = ts_sum(TR,window)/window;\n-(ts_delta(ATR, 1) * (window-1) + TR) / window",        "GPT输出的template": ""    },    {        "idea": "Rate of change (ROC)\nThe speed at which a variable changes over a specific period of time.",        "人工修改后的template": "p = 10;\nn = 5;\nROC = ts_delay({PRICE}, p) / ts_delay({PRICE}, p-c) * 100;\nROC",        "GPT输出的template": ""    },    {        "idea": "A stochastic oscillator is a momentum indicator comparing a particular closing price of a security to a range of its prices over a certain period of time. The sensitivity of the oscillator to market movements is reducible by adjusting that time period or by taking a moving average of the result. It is used to generate overbought and oversold trading signals, utilizing a 0–100 bounded range of values.",        "人工修改后的template": "C = {PRICE};\nL14 = ts_min(low,14);\nH14 = ts_max(high,14);\nK = (C-L14)/(H14-L14)*100;\n-K",        "GPT输出的template": ""    },    {        "idea": "On Balance Volume(OBV), 一个采用volume flow来预测股票变动的指标",        "人工修改后的template": "OBV = vec_avg({OBV});\nclose>ts_delay(close,1)? ts_delay(OBV,1) + volume : ts_delay(OBV,1) - volume",        "GPT输出的template": ""    },    {        "idea": "The Aroon Oscillator is a trend-following indicator that uses aspects of the Aroon Indicator (Aroon Up and Aroon Down) to gauge the strength of a current trend and the likelihood that it will continue.",        "人工修改后的template": "arr_up = (25 - ts_arg_max({HIGH PRICE}, 25))/25*100;\narr_down = (25 - ts_arg_min({LOW PRICE}, 25))/25*100;\narr_os = arr_up - arr_down\narr_os",        "GPT输出的template": ""    },    {        "idea": "平均方向指数The ADX makes use of a positive (+DI) and negative (-DI) directional indicator in addition to the trendline.\nThe trend has strength when ADX is above 25; the trend is weak or the price is trendless when ADX is below 20.\nNon-trending doesn't mean the price isn't moving. It may not be, but the price could also be making a trend change or is too volatile for a clear direction to be present.",        "人工修改后的template": "DH = {HIGH PRICE} - ts_delay({HIGH PRICE},1);\nDL = {LOW PRICE} - ts_delay({LOW PRICE},1);\nTR = max(high-low,abs(high-ts_delay(close,1)),abs(low-ts_delay(close,1)));\nDH_smooth = ts_sum(DH,14)/14;\nDL_smooth = ts_sum(DL,14)/14;\nTR_smooth = ts_sum(TR,14)/14;\nDIH = DH_smooth/TR_smooth*100;\nDIL = DL_smooth/TR_smooth*100;\nDX = abs(DIH-DIL)/abs(DIH+DIL)*100;\nfADX = ts_sum(DX,14)/14;\nADX = (ts_delay(fADX,1)*13+fADX)/14;\nADX",        "GPT输出的template": ""    },    {        "idea": "Supertrend Indicator用来预测趋势\nIt combines the average true range (ATR) with a multiplier to calculate its value.\nThis value is then added to or subtracted from the asset’s closing price to plot the supertrend line.\nWhen the supertrend line moves below price, it's often considered a buy signal, and when it moves above price, it's generally the time to sell.",        "人工修改后的template": "Multiplier = {const};\nwindow = {n};\nTR = max(high-low,abs(high-ts_delay(close,1)),abs(low-ts_delay(close,1)));\nATR = ts_sum(TR,window)/window;\nSupertrend=(High+Low)/2+(Multiplier)*(ATR);\nSupertrend",        "GPT输出的template": ""    },    {        "idea": "The Fisher Transform Indicator is a technical indicator that converts prices into a Gaussian normal distribution. It highlights when prices have moved to an extreme, based on recent prices. 对于价格变量，应调整到[-1,1]的范围内。",        "人工修改后的template": "X = s_log_1p({PRICE});\nFisher = 0.5*log((1+X)/(1-X));\nFisher",        "GPT输出的template": ""    },    {        "idea": "高风险日获得的收益往往是承担风险带来的，其更多源于投资者过度自信导致的价格反应过度，因此未来更倾向于反转效应，而低风险日获得的收益并不源于承担高风险，因此未来更偏向于动量效应，我们以时序均值调整后的风险指标来加权个股每日的溢价，并以此构建统一的动量反转因子。",        "人工修改后的template": "turnover = volume/(sharesout * 1000000);\nR1 = {REALIZED VOLATILITY};\nrisk = ts_mean(R1, 20) - R1;\nmkt_ret = vec_avg({CLOSE PRICE}) / ts_delay(vec_avg({CLOSE PRICE}), 1)-1;\nalpha = ts_rank(risk + (returns - mkt_ret), 20);\nalpha",        "GPT输出的template": "\"risk_premium = ts_mean(realized_volatility, 20) - realized_volatility; mkt_ret = vec_avg(close_price) / ts_delay(vec_avg(close_price), 1) - 1; alpha = ts_rank(risk_premium + (returns - mkt_ret), 20);\""    },    {        "idea": "多因子模型是一种在计算中使用多种因子来解释市场现象和/或均衡资产价格的金融模型。多因子模型可用于解释单个证券或证券组合。它通过比较两个或多个因子来分析变量之间的关系以及由此产生的表现。多因子模型用于构建具有某些特征（例如风险）的投资组合或跟踪指数。\nFama and French Six-Factor Model:\nRi - Rf = ai + βi (Rm – Rf) + si SMB + hi HML + ri RMW + ci CMA + wi WML + εi (9)\nwhere\nRi = Return of the asset i\nRf = Risk-free rate\nRm = Return of the market portfolio\nRm – Rf = Market risk premium\nSMB = Size premium, Small minus Big\nHML = Value premium, High minus Low\nRMW = Profitability premium, Robust minus Weak\nCMA = Investment premium, Conservative minus Aggressive\nWML = Momentum premium, Winner minus Loser\nαi = Intercept, Return that cannot be explained by factors\nβi, si, hi, ri, ci, wi = Factor coefficients of investment i",        "人工修改后的template": "market_ret = ts_delta(1+group_rank(fam_alpha_rank*returns, subindustry), 320)-3;\nrft = fnd6_optrfr;\nHML = ts_backfill(ts_delay(mdf_pbk,1), 350)/ts_backfill(ts_delay(fnd6_mkvalt,1),350);\nSMB = rank(cap);\nRMW = quantile(mdf_oey);\nCMA = (ts_delay(assets,1)-ts_delay(assets,2))/ts_delay(assets,2);\nMOM = ts_delay(returns, 252);\nexpected_return = rft+0.5*{BETA TO SPY}*(market_ret-0.5*rft) + 0.5*systematic_risk_last_360_days + (HML + SMB + RMW + CMA + MOM)*{BETA TO SPY};\nactual_return = ts_backfill(returns+1,320)-1;\nalpha = trade_when(ts_rank(ts_std_dev(returns, 24), 245) > 0.76, actual_return-expected_return, -1);\nts_backfill(alpha, 20)",        "GPT输出的template": " \"market_ret = ts_delta(1+group_rank(fam_alpha_rank*returns, subindustry), 320)-3;\\n rft = fnd6_optrfr;\\n HML = ts_backfill(ts_delay(mdf_pbk,1), 350)/ts_backfill(ts_delay(fnd6_mkvalt,1),350);\\n SMB = rank(cap);\\n RMW = quantile(mdf_oey);\\n CMA = (ts_delay(assets,1)-ts_delay(assets,2))/ts_delay(assets,2);\\n MOM = ts_delay(returns, 252);\\n expected_return = rft + beta_last_360_days_spy * (market_ret - rft) + (HML + SMB + RMW + CMA + MOM) * beta_last_360_days_spy;\\n actual_return = ts_backfill(returns + 1, 320) - 1;\\n alpha = trade_when(ts_rank(ts_std_dev(returns, 24), 245) > 0.76, actual_return - expected_return, -1);\\n ts_backfill(alpha, 20)\""    },    {        "idea": "具有高商誉对销售额比率的股票表现不如该比率低的股票，特别是在那些进行更复杂并购的公司和拥有过度自信CEO的公司中更为明显。",        "人工修改后的template": "a = ts_backfill({GOODWILL}, 63)/ts_backfill(sales,63);\nalpha = -ts_regression(a, ts_product(returns + 1, 63) - 1,512,lag = 1,rettype = 3);\nwinsorize(trade_when(vec_range(mws46_mcv)>0.5, group_zscore(winsorize(ts_backfill(alpha, 63), std=4), industry), -1))",        "GPT输出的template": "\"goodwill_to_sales_ratio = ts_backfill(GOODWILL, 63) / ts_backfill(sales, 63);\\nalpha = -ts_regression(goodwill_to_sales_ratio, ts_product(returns + 1, 63) - 1, 512, lag=1, rettype=3);\\ntrade_signal = trade_when(vec_range(MWCV) > 0.5, group_zscore(winsorize(ts_backfill(alpha, 63), std=4), industry), -1)\""    },    {        "idea": "该策略基于企业生命周期的收益分析，提出做多成熟公司并做空初创公司的投资组合。通过现金流数据和市场情绪的结合，初创公司由于较大的不确定性和较高的波动性，被认为未来表现较差，而成熟公司则因盈利稳定性被认为表现较好。",        "人工修改后的template": "firm_FCF = ts_quantile(mdl25_vrv421_81v,66,driver=gaussian, sigma=1.0);\nt = {t};\nerror = ts_regression({i},{j},t);\n\nalphaA = ts_regression(firm_FCF,error,t);\n\nalphaB = group_neutralize(firm_FCF,industry); \n\ncondA = cashflow_op<0 && cashflow_fin >0;\n\ncondB = cashflow_op>0 && cashflow_invst<0;\n\ncondC = ts_corr(alphaA,{k},126)<0 ;\n\ntrade_when(condB,rank(alphaB),condA&&condC?alphaA:nan)",        "GPT输出的template": "t = {d};\\ncash_flow_lifecycle = ts_mean(FCF t);\\ninst_ownership_rank = rank(mdl10_inst_ownership);\\nsentiment_factor = snt_buzz_ret;\\nintro_firm = bucket(rank(cash_flow_lifecycle) range=\\\"0.1 0.3\\\");\\nmature_firm = bucket(rank(cash_flow_lifecycle) range=\\\"0.7 0.9\\\");\\nsignal_long = group_neutralize(rank(mature_firm), industry);\\nsignal_short = group_neutralize(rank(intro_firm), industry);\\nalpha_signal = subtract(signal_long, signal_short)"    },    {        "idea": "在线评论的情绪和品牌忠诚度等非财务特征可显著预测个股长期回报。通过分析这些数据，尤其在八周的回报周期内，对个股的股票价格和长期表现进行了预测。",        "人工修改后的template": "average_sentiment = {i}\n\nlong_threshold = 0.5   \nshort_threshold = -0.5 \n\nsentiment_score = average_sentiment * returns\n\nalpha_signal = if_else(sentiment_score > long_threshold, 1, \n               if_else(sentiment_score < short_threshold, -1, 0))\nt1 = {t1};\nt2 = {t2};\nfinal_alpha = ts_backfill(group_rank(ts_rank(alpha_signal, t), industry), t2)\n\nfinal_alpha",        "GPT输出的template": "CLOSEW = ts_delay(close, 1);\\nOPENW = ts_delay(open, 1);\\nLOWW = ts_min(low, 5);\\nHIGHW = ts_max(high, 5);\\nweekly_return = (CLOSEW - OPENW) / OPENW;\\nsentiment_signal = ts_regression(weekly_return, social_media_sentiment, 5);\\nneutral_signal = group_neutralize(sentiment_signal, industry);\\nfinal_signal = rank(neutral_signal)"    },    {        "idea": "股票大跌前知情交易者急于离场，大量抛售股票，造成较高的换手率；而大涨前知情交易者急于进场，大量购买 股票，造成较高的换手率。换手率和股价的波动关系越强，则暗示信 息泄露程度越高，我们在该类股票上的博弈可能陷入劣势。",        "人工修改后的template": "turnover_nor=turnover-ts_mean(turnover,20);change_nor=change-ts_mean(change,20);-ts_corr(ts_delay(turnover,1),change,20)",        "GPT输出的template": "alpha(FR): scale_down(ts_corr(ts_rank(turnover, 20) - ts_mean(turnover, 20), price_change)) + w_u * scale_down(ts_corr(ts_rank(turnover, 20) - ts_mean(turnover, 20), price_change_if_up)) - w_d * scale_down(ts_corr(ts_rank(turnover, 20) - ts_mean(turnover, 20), price_change_if_down));"    },    {        "idea": "在交易过程中，那些股价突然发生跳跃上 涨的个股，会更容易吸引博彩偏好型投资者前来购买，犹如耀眼的火 光更容易吸引飞蛾，然而这些突然跳跃上涨的个股，往往由于被过分 关注而被超买，从而导致未来股价下跌。相反，那些真正基本面向好、股价能够持续 上涨的股票，其上涨的过程往往是均匀平缓非跳跃的，通过对股价变化过程的跳跃程度加以衡量，从而找出真正向好的股 票。",        "人工修改后的template": "compound_returns = log(close/open);jump = (close/open - 1 - compound_returns) * 2 - compound_returns ** 2;-jump",        "GPT输出的template": "group_mean(jump_decay(open, 1, sensitivity=0.5), group) - group_mean(smoothed_close, group) > quantile(group_std_dev(ts_mean(smoothed_close, 20), group), driver=uniform) ? 1 : (group_max(jump_decay(high, 1, sensitivity=0.5), group) - group_min(jump_decay(low, 1, sensitivity=0.5), group) < normalize(ts_std_dev(smoothed_close, 20), useStd=true) ? -1 : 0)"    },    {        "idea": "两种截然不同的投资者分别主导隔夜交易和盘中交易，而这两类投资者不同的交易逻辑可能会打起“拉锯战”。在进行价格矫正的过程中，机构投资者可能会出现过度矫正的情况，即将隔夜的一部分有效交易误认为是由于噪音产生的错误定价。过度矫正会使股票定价出现偏误，在未来有回归的趋势。",        "人工修改后的template": "ret_oc = close/open-1;ret_co = open/ts_delay(close,1)-1;NR = ts_sum(ret_co>0&&ret_oc<0?1:0,66)/66;PR = ts_sum(ret_co<0&&ret_oc>0?1:0,66)/66;rank(NR)+rank(-PR)",        "GPT输出的template": "FISH_PROFIT = AB_NR + AB_PR;\\nAB_NR = (NR / ts_mean(NR, 12)) - 1;\\nAB_PR = (PR / ts_mean(PR, 12)) - 1;\\nNR = IF(RET_INTRA < 0 AND RET_OVERNIGHT > 0, 1, 0);\\nPR = IF(RET_INTRA > 0 AND RET_OVERNIGHT < 0, 1, 0);\\nRET_INTRA = (OPEN - CLOSE[1]) / CLOSE[1];\\nRET_OVERNIGHT = (OPEN - CLOSE[1]) / (CLOSE[1] * (1 + RET_INTRA));"    },    {        "idea": "传统的资产定价模型通常假设投资者完全理性并且能够利用市场上所有的可得信 息， 但在实际投资中， 大多数投资者对资产进行定价时往往会受隐形的“心理权 重”影响。因此，基于凸显理论（Salience Theory）对投资者进行投资决策时的心理权重进行了定量描绘，构建了“凸显性收益”因子 STR，当 STR 为显著正时，投资者往往过度关注股票的上涨潜力，从而成为风险寻求者；当投资者过分关注股票的负收益并强调其下行风险时， STR 显著为负，相关的股票可能面临低估。构建凸显因子STR，再通过排序做多STR低的股票，做空STR高的股票执行策略。\n构建STR因子的方法如下：设定超参数theta为0.1设定超参数delta为0.7首先，按照如下方法计算系数sigma: （日度换手率-日度换手率市场均值）的绝对值/（日度换手率的绝对值 + 日度换手率市场均值的绝对值 + 超参数theta）建立窗口期天数22时序上用滚动窗口期给负的sigma进行排名，得到以在第几名为计量的排位，sigma在窗口期内的值越小，他的排名就越大（数值越高），额外处理排名返回值为0的情况：将之设为1，最终得到排名k_1。再次计算k_1在窗口期时序上的排名，然后对delta进行k_1的排名次方运算，得到的值再除以该值在窗口期的滚动均值，得到权重系数w。最后计算权重系数w与日度收益率在窗口期内的时序协方差，之后进行截面上的zscore运算，再进行市值中性化，得到STR因子。\n",        "人工修改后的template": "Delta = 0.7;Theta=0.1; mkt_turn = group_mean({turnover},1,market);\nSigma = abs({turn}-mkt_turn)/(abs({turn}) + abs(mkt_turn)+theta);days=22;\nK = ts_rank(-sigma,days)*days;k_1=if_else(k==1,1,k);weight = power(delta,ts_rank(k_1,days))/ts_mean(power(delta,ts_rank(k_1,days)),days);\nSTT = ts_covariance(weight, {returns},days);\ngroup_neutralize(zscore(-STT),bucket(rank(cap),range=”0,1,0.1”)\n",        "GPT输出的template": "sigma = abs(turnover - mean_turnover_market) / (abs(turnover) + abs(mean_turnover_market) + 0.1)\\nk_1 = ts_rank(-sigma, 22)\\nk_1 = if_else(k_1 == 0, 1, k_1)\\nk_2 = ts_rank(k_1, 22)\\nw = pow(k_2, 0.7) / ts_mean(pow(k_2, 0.7), 22)\\nSTR = ts_covariance(w, daily_return, 22)\\nSTR_zscore = zscore(STR)\\nSTR_neutralized = group_neutralize(STR_zscore, cap)"    },    {        "idea": "技术指标类因子。反转因子是常见因子，认为过去一段时间累计收益率好的会在未来出现反转。通过当日和前一日的收盘价构建对数收益率，然后计算过去一个月滚动窗口的累计收益率。这个因子是反向因子，做空值大的，做多值少的",        "人工修改后的template": "ret= log(close)-log(ts_delay(close,1));-ts_sum(ret,20)\n",        "GPT输出的template": "log_return = ts_returns(close, 1)\\ncumulative_return = ts_sum(log_return, 21)\\nsignal = rank(-cumulative_return)"    },    {        "idea": "Alpha Idea5:\n技术指标类因子。收益率峰度是常见因子，峰度高意味着方差增大是由低频度的大于或者小于平均值的极端差值引起的。这个因子是反向因子，做空值大的，做多值少的",        "人工修改后的template": "ret= log(close)-log(ts_delay(close,1));-ts_kurtosis(ret,20)",        "GPT输出的template": "log_return = ts_returns(close, 1)\\nkurtosis_return = ts_kurtosis(log_return, 21)\\nsignal = rank(-kurtosis_return)"    },    {        "idea": "该因子通过对不同时间窗口内的日均换手率、换手率偏离度及标准差进行分析，同时结合市值分组并进行市值中性化处理，降低市值对换手率因子的影响。通过这种方法，可以更加有效地识别市场中的流动性异常和过度交易行为。",        "人工修改后的template": "turnover = volume/(sharesout * 1000000);\nsignal = (ts_std_dev(turnover,5)/ts_std_dev(turnover,500))-1;\ngroup_neutralize(-signal,bucket(rank(cap), range='0.1,1,0.1')",        "GPT输出的template": "Alpha = group_neutralize(ts_std(turnover, window_n), bucket(rank(cap), range='0.1,1,0.1'))\""    },    {        "idea": "通过计算隔夜涨跌幅的绝对值与昨日换手率的相关系数，结合市值中性化处理来识别潜在的市场信息优势。若相关性较高，表明在高换手率的交易日，有较大的价格波动，可能反映了知情交易的存在，市场效率较低，适合做空。",        "人工修改后的template": "overnight_ret = (open - ts_delay(close,1))/ts_delay(close,1);\nabs_ove_ret = abs(overnight_ret);\nturn = volume/sharesout;\nturn_d1 = ts_delay(turn,1);\ncorr = ts_corr(abs_ove_ret , turn_d1,7);\n-corr",        "GPT输出的template": "Alpha = group_neutralize(ts_corr(abs(ts_delta(data1, window1)), data2 / ts_delay(data2, window2)), bucket(rank([cap]), range='0.1,1,0.1'))"    },    {        "idea": "通过均线的收敛与发散来捕捉市场中的变盘点信号。均线收敛表明市场筹码分布集中，未来个股方向选择的概率增加。通过计算不同周期价格和成交量均线的标准差，能够识别未来股价可能的变动方向。价量双收敛因子能有效弥补单一因子的不足，并提升预测效果。",        "人工修改后的template": "ma1 = ts_mean(close,1);\nma5 = ts_mean(close,5);\nma20 = ts_mean(close,20);\nma60=ts_mean(close,60);\nma120 = ts_mean(close,120);\nstd = sqrt(((ma1-mean)^2+(ma5-mean)^2+(ma20-mean)^2+(ma60-mean)^2+(ma120-mean)^2/5)\n ",        "GPT输出的template": "\"price_convergence\": \"Alpha = -log(1 + ts_std([price_ma_1, price_ma_5, price_ma_10, price_ma_20, price_ma_60, price_ma_120]))\",\n    \"volume_convergence\": \"Alpha = -log(1 + ts_std([volume_ma_1, volume_ma_5, volume_ma_10, volume_ma_20, volume_ma_60, volume_ma_120]))\",\n    \"price_volume_convergence\": \"Alpha = (price_convergence + volume_convergence) / 2\""    },    {        "idea": "通过历史分位数计算某个指标在一段时间区间中的表现，并结合多种中性化处理（如交易所、市场、行业等），进一步提升因子的稳定性和适应性。这种因子不仅能反映市场中的变化趋势，还能剔除不同市场环境下的系统性偏差。",        "人工修改后的template": "group_neutralize(ts_rank(mdl175_roe,375),bucket(rank(cap),range='0.1,1,0.1'))",        "GPT输出的template": "Alpha = group_neutralize(ts_rank(data1, window1), bucket(rank([neutralize_by]), range='0.1,1,0.1'))"    },    {        "idea": "通过计算隔夜回报与日内回报的时间序列均值，并对这些回报与市场风险因素进行回归分析，剔除市场中多空双方竞争带来的噪声信号，预测未来的收益。回归分析结合多个变量和时间窗口，可以更好地捕捉市场情绪波动并评估未来的横截面收益。",        "人工修改后的template": "ov_ret = (open-ts_delay(close,1))/ts_delay(close,1);\ndayin_rev = (close-open)/open;\nts_corr(ov_ret,dayin_rev,dayin_rev,5)",        "GPT输出的template": "Alpha = ts_regression(rank(ts_mean(data1, window1)), rank(ts_mean(data2, window2)), group_mean(risk_factors,windows3), window4)"    },    {        "idea": "技术指标类因子。收益率偏度是常见因子，偏度为负数，意味着大多数的值位于平均值的右侧，偏度为正意味着大多数的值位于平均值的左侧。这个因子是负向因子。见因子，峰度高意味着方差增大是由低频度的大于或者小于平均值的极端差值引起的。这个因子是反向因子，做空值大的，做多值少的",        "人工修改后的template": "ret= log(close) - log(ts_delay(close,1)); -ts_skewness(ret,20)",        "GPT输出的template": "log_return = ts_returns(close, 1)\\nskewness_return = ts_skewness(log_return, 21)\\nsignal = rank(-skewness_return)"    },    {        "idea": "波动反转因子 概括来说：如果对确定的事物，人们通常会猜测反转；对不确定的事物，人们通常会猜测动量，即与最近一期的结果相同 计算最近20 天的日间收益率的均值和标准差，作为当月的日内（日间）收益率和日内收益率（日间）的波动率。 如果日内收益率的波动率 < 市场截面均值，则说明是确定的事务，猜测反转，使用 -1*当月的日内（日间）收益率，否则使用当月的日内（日间）收益率",        "人工修改后的template": "d1_mean = ts_mean(close/ts delay(close,1)-1,20)\nd1_std = ts std dev(close/ts delay(close,1)-1,20);\nd1 mkt mean = group mean(d1 std,1,market),\nd1 std rev = dl std<d1 mkt mean?-d1 mean:dl mean;",        "GPT输出的template": "log_return = ts_returns(close, 1)\\nmean_return = ts_mean(log_return, 20)\\nstd_return = ts_std_dev(log_return, 20)\\nmean_market = group_mean(mean_return, industry)\\nstd_market = group_mean(std_return, industry)\\nsignal = if_else(std_return < std_market, -mean_return, mean_return)"    },    {        "idea": "波动反转因子 概括来说：如果对确定的事物，人们通常会猜测反转；对不确定的事物，人们通常会猜测动量，即与最近一期的结果相同 计算最近20 天的日间收益率的均值和标准差，作为当月的日内（日间）收益率和日内收益率（日间）的波动率。 如果日内收益率的波动率 < 市场截面均值，则说明是确定的事务，猜测反转，使用 -1*当月的日内（日间）收益率，否则使用当月的日内（日间）收益率",        "人工修改后的template": "night ret = open/ts delay(close,1),night mkt mean = group_mean(night ret,1,market)night dist = abs(night ret - night mkt mean);dist mean = ts mean(night dist,20);dist std = ts std dev(night dist,20);night mean = group mean(dist std,1,market);night std rev = dist std<night mean?-dist mean:dist mean;",        "GPT输出的template": "overnight_return = (open - close[t-1]) / close[t-1]\\novernight_distance = abs(overnight_return - group_mean(overnight_return, industry))\\nmean_distance = ts_mean(overnight_distance, 20)\\nstd_distance = ts_std_dev(overnight_distance, 20)\\nmean_market = group_mean(mean_distance, industry)\\nstd_market = group_mean(std_distance, industry)\\nsignal = if_else(std_distance < std_market, -mean_distance, mean_distance)"    },    {        "idea": "该研报的核心思想就是把过去20日的returns按照小单交易占比排序，分为5组，每组求平均，然后用第一组的平均-第五组的平均作为量化因子。\nbrain平台alpha建立思想：\n1、用Brain平台上可用的小买单/卖单数据代替交易占比数据\n2、在横截面上减去行业平均值，做因子的行业中性化\n3、进一步考虑，大小单与公司市值相关，因此做市值中性化\n4、“小单”因子有负向预测能力，因此取负号",        "人工修改后的template": "small_sell=-vec_percentage(pv27_sell_value_small_order,percentage=0.8);\nsmall_buy=-vec_percentage(pv27_buy_value_small_order,percentage=0.8);\nfac=small_sell+small_buy;\nfac_diff_mean=rank(fac-group_mean(fac,1,subindustry));\ngroup_neutralize(fac_diff_mean,bucket(rank(cap),range='0.1,1,0.1'))",        "GPT输出的template": "# 定义窗口期\nd = 252;\n\n# 处理小买单和小卖单数据\nsmall_buy_value = vec_sum(pv27_buy_value_small_order);  # 小买单总金额\nsmall_sell_value = vec_sum(pv27_sell_value_small_order);  # 小卖单总金额\n\n# 计算小单交易总额\ntotal_small_order_value = small_buy_value + small_sell_value;\n\n# 行业中性化\nindustry_group = industry;  # 假设我们使用行业作为分组\nindustry_mean = group_mean(total_small_order_value, 1, industry_group);  # 传入权重1\nindustry_neutralized = total_small_order_value - industry_mean;\n\n# 市值中性化\ncap_group = bucket(rank(cap), range='0,1,0.1');  # 将市值分成若干组\ncap_mean = group_mean(industry_neutralized, 1, cap_group);  # 传入权重1\ncap_neutralized = industry_neutralized - cap_mean;\n\n# 取负号\nfactor = -cap_neutralized;\n\n# 输出最终因子\nfactor"    },    {        "idea": "文章提出了一种衡量公司质量的方法，即在压力时期，高质量的公司表现更好。作者使用了股票回报率作为衡量公司质量的指标，并通过对行业内公司的回报率进行排序，将公司分为压力时期稳定（SS）和脆弱（SV）两类。研究发现，SS公司相比于SV公司在盈利能力、投资策略、财务风险、市场风险和特异波动性等方面表现更好。",        "人工修改后的template": "group_rank(log(ts_min(returns,20)/ts_min(group_mean(returns,volume,densify(sector)),20)),densify(sector))",        "GPT输出的template": "# 定义时间窗口\nd = 21;  # 例如，21个交易日代表一个月\n\n# 计算每只股票在指定时间窗口内的最小回报率\nstock_worst_return = ts_min(returns, d);\n\n# 按照行业分组（可以使用 industry, sector, 或 subindustry）\nindustry_group = industry;  # 可以替换为 sector 或 subindustry\n\n# 计算每个行业组在相同时间窗口内的最小回报率的加权平均值\nindustry_worst_mean_return = group_mean(ts_min(returns, d), volume, industry_group);\n\n# 将每只股票的最小回报率除以所在行业的最小回报率的加权平均值\nfactor = stock_worst_return / industry_worst_mean_return;\n\n# 中性化处理（可选）\nneutralized_factor = factor - group_mean(factor, volume, industry_group);"    },    {        "idea": "本文讨论了期权的估值逻辑以及四种选定的实物估值方法。 根据他们的建模选择进行选择。 所选方法中的两种，DatarMathews 方法 [16, 24] 和模糊支付方法 [13] 代表了后来的发展 实物期权估值以及 Black & Scholes 公式和期权二项式模型 对实物期权估值中使用的更成熟的方法进行定价。 本文的目的是了解所使用的实物期权估值模型的总体情况 今天并讨论未来的建模观点。Datar–Mathews 方法[1]（DM 方法）[2] 是一种实物期权估值方法。 该方法提供了一种简单的方法，只需使用项目积极结果的平均值即可确定项目的实际期权价值。 该方法可以理解为净现值（NPV）多情景蒙特卡罗模型的扩展，并针对风险规避和经济决策进行了调整。 该方法使用标准贴现现金流 (DCF) 或 NPV、项目财务评估中自然产生的信息。 它由西雅图大学教授 Vinay Datar 于 2000 年创建； 斯科特·H·马修斯 (Scott H. Mathews)，波音公司技术研究员。",        "人工修改后的template": "C=max(vec_avg(anl69_best_opp)-forward_price_20,0);\ngroup_neutralize(rank(C),bucket(rank(cap),range='0,1,0.1'))\n或\nnvg=(anl69_best_cps_numest+anl69_best_cps_hi+anl69_best_cps_lo)/3;\nC=max(nvg,0);\ngroup_neutralize(rank(C),bucket(rank(cap),range='0,1,0.1'))",        "GPT输出的template": "d = 252; # 过去一年的窗口期\nlookback = 14; # RSI回看天数\n\n# 使用线性回归预测未来的日度收益率\nfuture_return_estimate = ts_regression(returns, volume, d, rettype=5); # 使用rettype=5获取Sum of Squares of Total (SST)\n\n# 计算未来价值\nfuture_value = close * (1 + future_return_estimate);\n\n# 处理每股现金流数据\ncash_flow_per_share = vec_avg(anl69_best_cps_numest); # 使用vec_avg处理vector数据\n\n# 定义现金流预测\n# 假设未来一段时间（如一年）的现金流等于当前预测的未来价值\ncash_flow = future_value * cash_flow_per_share;\n\n# 计算期望值\nexpected_value = cash_flow;\n\n# 计算排名\nvalue_rank = group_rank(expected_value, industry); # 使用行业作为分组\n\n# 构建投资组合权重\nweight = value_rank; # 将排名转换为-1到1之间的值\n\n# 结合其他因素调整权重\ncombined_weight = weight; # 仅使用排名作为权重\n\n# 市值中性化处理\ncap_group = bucket(rank(cap), range='0,1,0.1');\ncombined_weight1 = group_neutralize(combined_weight, cap_group);\n\n# 缩放权重\nscale(combined_weight1)  # 直接输出缩放后的权重"    },    {        "idea": "做多低beta的股票，做空高beta的股票",        "人工修改后的template": "beta_z = rank(beta_last_360_days_spy);\n\n\nweight = 0.6 - beta_z;\n\n\n\n\nscale(weight) ",        "GPT输出的template": "beta = cov(returns, market_returns) / var(market_returns); # 计算个股相对于市场的贝塔值\n   rank_beta = rank(beta); # 对贝塔值进行排序\n   low_beta_portfolio = if_else(rank_beta < 0.3, 1, 0); # 构建低贝塔组合\n   high_beta_portfolio = if_else(rank_beta > 0.7, 1, 0); # 构建高贝塔组合\n   low_beta_weighted = low_beta_portfolio * market_cap; # 低贝塔组合按市值加权\n   high_beta_weighted = high_beta_portfolio * market_cap; # 高贝塔组合按市值加权\n   total_low_beta = ts_sum(low_beta_weighted, 1); # 低贝塔组合总市值\n   total_high_beta = ts_sum(high_beta_weighted, 1); # 高贝塔组合总市值\n   low_beta_weighted = low_beta_weighted / total_low_beta; # 低贝塔组合归一化权重\n   high_beta_weighted = high_beta_weighted / total_high_beta; # 高贝塔组合归一化权重\n   bab_factor = low_beta_weighted - high_beta_weighted; # 做多低贝塔，做空高贝塔\n   bab_factor = group_neutralize(bab_factor, bucket(rank(market_cap), range='0.1,1,0.1')); # 市值中性化处理\n   alpha = returns * bab_factor - risk_free_rate; # 计算超额收益"    },    {        "idea": "第一，做多低beta的股票，做空高beta的股票。\n第二，文章发现流动性限制对BAB有明显负影响，当流动性收紧，低beta资产收益下降较高beta资产更多，BAB因子的收益率降低。\n\n启示：BAB因子收益与资产流动性正相关，可以在低beta资产中增加高流动性资产的权重。进一步研究基于该论文的其他研究，发现论文A Market-Based Funding Liquidity Measure提出了流动性代表指标，包括特质风险，Amihud流动性指标，机构持有比率，分析师覆盖，以及市值。因为波动率也是流动性指标之一，加入短期波动率更容易捕捉流动性的变动。因此加入波动率，特质风险，Amihud流动性指标和市值指标，并选取改善最好的组合，看信号是否有改善。\n\n",        "人工修改后的template": "volatility_st = ts_std_dev(returns < 0 ? returns: 0, 20);\n\n\nilliquidity = ts_decay_exp_window(abs(returns)/volume, 252);\n\n\nidiosyncratic = ts_decay_exp_window(power(ts_regression(scale_down(returns), scale_down(beta_last_360_days_spy), 252, lag=0, rettype=4)/252, 0.5), 252);\n\n\n\n\nbeta_z = rank(beta_last_360_days_spy);\n\n\nweight = 0.6 - beta_z;\n\n\n\n\nscale(weight) * scale_down(rank(-volatility_st)) * scale_down(rank(-illiquidity)) * scale_down(rank(-idiosyncratic)) \n",        "GPT输出的template": "beta_z = rank(beta_last_360_days_spy); # 计算贝塔值排名\n   idiosyncratic_risk_z = rank(idiosyncratic_risk); # 计算特质风险排名\n   amihud_liquidity_z = rank(amihud_liquidity); # 计算Amihud流动性指标排名\n   short_term_volatility_z = rank(short_term_volatility); # 计算短期波动率排名\n   market_cap_z = rank(market_cap); # 计算市值排名\n   \n   # 综合流动性指标\n   liquidity_score = (idiosyncratic_risk_z + amihud_liquidity_z + short_term_volatility_z + market_cap_z) / 4;\n   \n   # 结合贝塔值和流动性指标\n   combined_score = 0.6 * (1 - beta_z) + 0.4 * liquidity_score; # 低贝塔且高流动性资产得分较高\n   \n   # 构建投资组合权重\n   weight = if_else(combined_score > 0.5, 1, -1); # 高分为正权重，低分为负权重\n   \n   # 市值中性化处理\n   cap_group = bucket(rank(market_cap), range='0.1,1,0.1');\n   weight = group_neutralize(weight, cap_group);\n   \n   # 缩放权重\n   scaled_weight = scale(weight);"    },    {        "idea": "当融资流动性风险提高的时候， 所有资产的Beta均会倾向于向1压缩。",        "人工修改后的template": "weight = 0.6 - beta_z;",        "GPT输出的template": "alpha_signal = group_mean(vec_avg(ts_corr(fund_flow, market_indices, lookback=30), sector), industry_group)"    },    {        "idea": "有杠杆限制的投资人会选择高beta的资产组合，而杠杆限制较少的投资人会选择带杠杆的低beta资产组合。",        "人工修改后的template": "",        "GPT输出的template": ""    },    {        "idea": "企业倍数是股票回报的重要决定因素；企业倍数低的公司倾向于具有较高的贴现率和较高的后续股票回报。企业倍数 = 企业价值/EBITDA = (股权价值+债务+优先股-现金)/EBITDA，\n\n而EBITDA是指Earnings Before Interest, Taxes, Depreciation and Amortization，即税息折旧和摊销前利润。",        "人工修改后的template": "",        "GPT输出的template": "alpha_signal = group_operators(ts_corr(enterprise_multiple, stock_returns, lookback=36), industry_group)"    },    {        "idea": "默顿模型可以更轻松地对公司进行估值，并通过分析其债务到期日和债务总额来帮助分析确定该公司是否能够保留偿付能力。\n\n默顿模型计算欧式看跌期权和看涨期权的理论定价，不考虑期权有效期内支付的股息。 然而，该模型可以通过计算标的股票的除息日价值来考虑股息。",        "人工修改后的template": "",        "GPT输出的template": "default_prob = Φ((log(DT/A0) - (r - σA^2/2) * T) / (σA * sqrt(T)))"    },    {        "idea": "原研报逻辑\n1.利用逐笔成交数据，标记主动买卖单\n2.根据主动买卖，确定总体方向\n3.与收盘价比较，确定总体盈亏占比\n使用数据\n成交量/价 ，买卖单号 共四列数据\n偏差：数据颗粒度小，细节更多\n构建近似逻辑\n1.利用ohclv数据，将涨跌情况，标记为主动买卖单占比\n2.根据涨跌方向，确定总体方向\n3.与当前收盘价比较，确定相对当前盈亏占比\n使用近似数据\n开高收低成交量\n偏差：数据颗粒度大，损失价格变动细节",        "人工修改后的template": "",        "GPT输出的template": "Regret_Avoidance_Factor = if(close < buy_price, HCVOL + HCPE, if(close > sell_price, LCVOL + LCPE, 0))"    }]
```

---

## 讨论与评论 (5)

### 评论 #1 (作者: WL13229, 时间: 1 year ago)

主要贡献者

- **[DL25631](/hc/en-us/profiles/22807235985431-DL25631)**
- **[FL11741](/hc/en-us/profiles/18515544005527-FL11741)**
- **[TL87739](/hc/en-us/profiles/22586762204183-TL87739)**
- **[ZL29184](/hc/en-us/profiles/22955594228503-ZL29184)**
- **[SD72354](/hc/en-us/profiles/22860160233367-SD72354)**
- **[MC63847](/hc/en-us/profiles/22297304707095-MC63847)**
- **[YH69102](/hc/en-us/profiles/18490343218711-YH69102)**
- **[CW60999](/hc/en-us/profiles/13836923085207-CW60999)**

---

### 评论 #2 (作者: WL13229, 时间: 1 year ago)

建议复制粘贴至代码编辑器进行观看

---

### 评论 #3 (作者: MH33574, 时间: 1 year ago)

```
"idea": "企业倍数是股票回报的重要决定因素；企业倍数低的公司倾向于具有较高的贴现率和较高的后续股票回报。企业倍数 = 企业价值/EBITDA = (股权价值+债务+优先股-现金)/EBITDA，\n\n而EBITDA是指Earnings Before Interest, Taxes, Depreciation and Amortization，即税息折旧和摊销前利润。",        "人工修改后的template": "",        "GPT输出的template": "alpha_signal = vec_avg(ts_corr(enterprise_multiple, stock_returns, lookback=36), industry_group)"这里是不是该是group_netrulization 而不是vec_avg？
```

---

### 评论 #4 (作者: KJ42842, 时间: 1 year ago)

@MH33574，对的，或者其他的group ops。

---

### 评论 #5 (作者: WJ40623, 时间: 7 months ago)

感觉人工模版和GPT输出的差异好大呀

---

