# [代码分享] 获取没有提交过的数据集代码优化

- **链接**: [代码分享] 获取没有提交过的数据集代码优化.md
- **作者**: 顾问 FD69320 (Rank 34)
- **发布时间/热度**: 11个月前, 得票: 64

## 帖子正文

背景随着定级的变化，很多伙伴的数据集权限也随之发生了变化。那么我们可能很想知道自己提交过哪些数据集，还有哪些数据集还没有涉猎，那么可以在新的赛季以更高的优先级去跑。如果去尝试获取所有提交过的alpha并分析表达式的话，对于不熟悉ast的伙伴们可能会有一些麻烦。这里分享一个比较简单的代码实现逻辑。实践方法我们可以通过Super Alpha的 (own) && in(datasets, '{dataset_id}') 的结果来快速地获取某一个dataset在我们所有提交的alpha中的情况代码调用Super Alpha SelectionBRAIN_API_SELECTION = /simulations/super-selectionself._ss_mgr = httpx.Clientdef super_alpha_selection(self,selection: str,instrument_type: str='EQUITY',region: str='USA',delay: int=1,limit: int=50,selection_limit: int=50,selection_handling: str=SUPER_ALPHA_SELECTION_HANDLING_POSITIVE,):"""Check whether the selection is valid.Args:selection (str):            The expression to select regular alphas to be included.instrument_type (str):      The instrument type.region (str):               The region.delay (int):                The delay.selection_limit (int):      The selection limit.selection_handling (str):   The selection handling method.Returns:pd.DataFrame:               The selected alphas."""params = {'instrumentType':       instrument_type,'region':               region,'delay':                str(delay),'selection':            selection,'limit':                str(limit),'selectionLimit':       str(selection_limit),'selectionHandling':    selection_handling,}try:r = self._ss_mgr.get(BRAIN_AIP_HOST + BRAIN_API_SELECTION, params=params).json()return pd.DataFrame(r['results'])except Exception as e:print(f"Failed to get datasets: {e}")return pd.DataFrame()判断是否提交过def is_dataset_submitted(self,dataset_id: str,region: str,universe: str,delay: int,):"""Check if a dataset has been submitted.Args:dataset_id (str):   The dataset ID to check.region (str):       The region of the dataset.universe (str):     The universe of the dataset.delay (int):        The delay of the dataset.Returns:bool: True if submitted, False otherwise."""result = Trueselection = BRAIN_DATASET_SUBMITTED_SELECTION.format(dataset_id=dataset_id)alphas = self.super_alpha_selection(selection, region=region)if alphas.empty:result = Falseelse:alphas['region'] = alphas['settings'].apply(lambda x: x['region'])alphas['delay'] = alphas['settings'].apply(lambda x: x['delay'])alphas['universe'] = alphas['settings'].apply(lambda x: x['universe'])if alphas.loc[(alphas['region'] == region) & (alphas['delay'] == delay) & (alphas['universe'] == universe)].empty:result = Falsereturn result更多应用这个方式的好处在于它可以不但可以快速地告诉我有哪些数据集已经跑跑过了，同时也能提示我跑过的数据集，有多少提交过的alpha，通过这些信息我们可以尽可能地平衡我们的多样性。

---

## 讨论与评论 (1)

### 评论 #1 (作者: LX71036, 时间: 9个月前)

你好大佬，都不知道自己提交过哪些数据集，这个帖子正好解决了我的问题，可是代码中有些我不太懂。就是其中的selection_handling: str=SUPER_ALPHA_SELECTION_HANDLING_POSITIVE,):以及r = self._ss_mgr.get(BRAIN_AIP_HOST + BRAIN_API_SELECTION, params=params).json(这两个部分我这边会报错，是不是因为这两个是需要去获取API的，然后上方的代码其实是没有给出来的吧？还希望大佬能够解答一下，谢谢！

---

