# 【新人向-SA的本地计算SC】复制即用！跟上后一篇PC检测代码！代码优化

- **链接**: https://support.worldquantbrain.com[Commented] 【新人向-SA的本地计算SC】复制即用跟上后一篇PC检测代码代码优化_38081199177239.md
- **作者**: LX57490
- **发布时间/热度**: 4个月前, 得票: 117

## 帖子正文

[【新人向-SA的Prod检测 —— 24h可检测600个！】即插即用！结合上一篇SA自相关文章！ – WorldQuantBrain-CN]([L2] 【新人向-SA的Prod检测  24h可检测600个】即插即用结合上一篇SA自相关文章_37554208805911.md)

#### 上述链接跳转PC检测帖子！

## **一、登录**

```
478,479行账号密码def main():    # 配置参数    class cfg:        username = ""        password = ""        data_path = Path('.')
```

## **二、配置**

```
# ===== 参数设置 =====# SuperAlpha专用参数START_DATE = "01-30"  # 起始日期（MM-DD格式）END_DATE = "02-01"  # 结束日期SHARPE_THRESHOLD = 5.0  # 夏普比率阈值FITNESS_THRESHOLD = 5.0  # 适应度阈值REGION = "IND"  # 区域ALPHA_NUM = 2000  # Alpha数量SELF_CORR_THRESHOLD = 0.3  # 低自相关阈值MAX_WORKERS_SELF_CORR = 3  # 自相关计算的最大线程数
```

SELF_CORR_THRESHOLD = 0.3  # 低自相关阈值  会给符合的SA打上名字和更改颜色！

## **三、完整代码**

**最后祝各位顾问月月VF1.0！RA60刀！SA60！日日120刀！！！！**

**高筑墙！广积粮！缓称王！**

```
import requestsimport pandas as pdimport loggingimport timeimport picklefrom collections import defaultdictimport numpy as npfrom tqdm import tqdmfrom pathlib import Pathfrom concurrent.futures import ThreadPoolExecutor, as_completedimport osimport jsonfrom datetime import datetime, timedeltaimport sysimport randomfrom requests.adapters import HTTPAdapterfrom urllib3.util.retry import Retryfrom typing import List, Dict, Optional, Tuple, Unionimport traceback# ===== 参数设置 =====# SuperAlpha专用参数START_DATE = "01-30"  # 起始日期（MM-DD格式）END_DATE = "02-01"  # 结束日期SHARPE_THRESHOLD = 5.0  # 夏普比率阈值FITNESS_THRESHOLD = 5.0  # 适应度阈值REGION = "IND"  # 区域ALPHA_NUM = 2000  # Alpha数量SELF_CORR_THRESHOLD = 0.3  # 低自相关阈值MAX_WORKERS_SELF_CORR = 3  # 自相关计算的最大线程数# 获取当前脚本所在目录script_dir = os.path.dirname(os.path.abspath(__file__))# 输出文件路径（与代码同目录）OUTPUT_FILE = os.path.join(script_dir, f"Superalpha_results_{START_DATE}_{REGION}.xlsx")# ===== 登录函数 =====def sign_in(username, password):    retry_strategy = Retry(        total=3,        backoff_factor=1,        status_forcelist=[500, 502, 503, 504]    )    adapter = HTTPAdapter(max_retries=retry_strategy)    s = requests.Session()    s.mount("https://", adapter)    s.mount("http://", adapter)    s.auth = (username, password)    max_try = 3    retry = 0    while retry < max_try:        try:            response = s.post('https://api.worldquantbrain.com/authentication')            if response.status_code in [200, 201]:                logging.info("登录成功")                return s        except Exception as e:            logging.error(f"登录失败: {e}")            time.sleep(5)        retry += 1    return None# ===== 数据存储函数 =====def save_obj(obj: object, name: str) -> None:    with open(name + '.pickle', 'wb') as f:        pickle.dump(obj, f, pickle.HIGHEST_PROTOCOL)def load_obj(name: str) -> object:    try:        with open(name + '.pickle', 'rb') as f:            return pickle.load(f)    except FileNotFoundError:        logging.warning(f"文件不存在: {name}.pickle")        return None    except Exception as e:        logging.error(f"加载对象失败: {e}")        return None# ===== 获取SuperAlpha的函数 =====def get_submit_alphas(session, start_date, end_date, sharpe_th, fitness_th, region, alpha_num):    """获取可以提交的SuperAlpha信息"""    output = []    count = 0    # 分页获取数据    for i in range(0, alpha_num, 100):        print(f"处理偏移量: {i}")        base_url = f"https://api.worldquantbrain.com/users/self/alphas?limit=100&offset={i}&status=UNSUBMITTED%1FIS_FAIL&dateCreated%3E=2026-{start_date}T00:00:00-05:00&dateCreated%3C=2026-{end_date}T00:00:00-05:00&is.fitness%3E={fitness_th}&is.sharpe%3E={sharpe_th}&settings.region={region}&order=-is.sharpe&hidden=false&type=SUPER"        try:            response = session.get(base_url)            if response.status_code == 200:                alpha_list = response.json().get("results", [])                for alpha in alpha_list:                    alpha_id = alpha.get("id")                    name = alpha.get("name")                    dateCreated = alpha.get("dateCreated")                    sharpe = alpha.get("is", {}).get("sharpe")                    fitness = alpha.get("is", {}).get("fitness")                    turnover = alpha.get("is", {}).get("turnover")                    margin = alpha.get("is", {}).get("margin")                    longCount = alpha.get("is", {}).get("longCount")                    shortCount = alpha.get("is", {}).get("shortCount")                    decay = alpha.get("settings", {}).get("decay")                    alpha_type = alpha.get("type", "REGULAR")                    # 中性化设置                    neutralization = alpha.get("settings", {}).get("neutralization", "NONE")                    neutralization_map = {                        "SUBINDUSTRY": "Subindustry",                        "STATISTICAL": "Statistical",                        "SLOW": "Slow Factors",                        "SLOW_AND_FAST": "Slow + Fast Factors",                        "SECTOR": "Sector",                        "NONE": "None",                        "MARKET": "Market",                        "INDUSTRY": "Industry",                        "FAST": "Fast Factors",                        "CROWDING": "Crowding Factors",                        "COUNTRY": "Country/Region",                        "SUPER_NEUTRAL": "Super Alpha Neutralization"                    }                    neutralization_name = neutralization_map.get(neutralization, neutralization)                    count += 1                    # 检查是否通过检查                    checks = alpha.get("is", {}).get("checks", [])                    checks_df = pd.DataFrame(checks)                    check_status = "Check FAIL"                    if not checks_df.empty and "result" in checks_df.columns:                        if not any(checks_df["result"].eq("FAIL")) and ((longCount or 0) + (shortCount or 0) > 100):                            check_status = "Check OK"                    # 构造记录                    rec = {                        "alpha_id": alpha_id,                        "alpha_type": alpha_type,                        "check_status": check_status,                        "sharpe": sharpe,                        "turnover": f"{turnover:.2%}" if turnover is not None else None,                        "fitness": fitness,                        "margin": f"{margin * 10000:.2f}‱" if margin is not None else None,                        "longCount": longCount,                        "shortCount": shortCount,                        "dateCreated": dateCreated,                        "decay": decay,                        "neutralization": neutralization,                        "neutralization_name": neutralization_name                    }                    if check_status == "Check OK":                        output.append(rec)            else:                print(f"请求失败，状态码: {response.status_code}")                print(f"响应内容: {response.text}")        except Exception as e:            print(f"处理偏移量 {i} 时出错: {e}")    print(f"总计处理 SuperAlpha 数量: {count}")    print(f"符合条件的 SuperAlpha 数量: {len(output)}")    return output# ===== 自相关计算核心函数 =====def wait_get(session, url: str, max_retries: int = 10) -> Optional[requests.Response]:    retries = 0    while retries < max_retries:        try:            response = session.get(url)            if "Retry-After" in response.headers:                retry_after = float(response.headers["Retry-After"])                time.sleep(retry_after)                continue            return response        except Exception as e:            print(f"请求失败: {e}")            time.sleep(2 ** retries)            retries += 1    return Nonedef _get_alpha_pnl(session, alpha_id: str) -> pd.DataFrame:    """获取指定alpha的PnL数据"""    pnl = wait_get(session, f"https://api.worldquantbrain.com/alphas/{alpha_id}/recordsets/pnl")    if pnl is None or pnl.status_code != 200:        return pd.DataFrame()    try:        pnl_data = pnl.json()        df = pd.DataFrame(pnl_data['records'],                          columns=[item['name'] for item in pnl_data['schema']['properties']])        df = df.rename(columns={'date': 'Date', 'pnl': alpha_id})        return df[['Date', alpha_id]]    except Exception as e:        print(f"解析PnL数据失败: {e}")        return pd.DataFrame()def get_alpha_pnls(session, alphas: List[Dict], alpha_pnls: Optional[pd.DataFrame] = None,                   alpha_ids: Optional[Dict[str, List[str]]] = None) -> Tuple[Dict[str, List[str]], pd.DataFrame]:    """获取多个alpha的PnL数据"""    if alpha_ids is None:        alpha_ids = defaultdict(list)    if alpha_pnls is None:        alpha_pnls = pd.DataFrame()    new_alphas = [item for item in alphas if item['id'] not in alpha_pnls.columns]    if not new_alphas:        return alpha_ids, alpha_pnls    for item_alpha in new_alphas:        alpha_ids[item_alpha['settings']['region']].append(item_alpha['id'])    # 使用线程池获取PnL数据    def fetch_pnl(alpha_id):        return _get_alpha_pnl(session, alpha_id).set_index('Date')    with ThreadPoolExecutor(max_workers=10) as executor:        results = list(executor.map(fetch_pnl, [item['id'] for item in new_alphas]))    # 合并结果    valid_results = [df for df in results if not df.empty]    if valid_results:        # 确保所有列名都是字符串        for df in valid_results:            df.columns = df.columns.astype(str)        alpha_pnls = pd.concat([alpha_pnls] + valid_results, axis=1)        alpha_pnls.sort_index(inplace=True)    return alpha_ids, alpha_pnlsdef get_os_alphas(session, limit: int = 100, get_first: bool = False) -> List[Dict]:    """获取OS阶段的alpha列表"""    fetched_alphas = []    offset = 0    total_alphas = 100    while len(fetched_alphas) < total_alphas:        url = f"https://api.worldquantbrain.com/users/self/alphas?stage=OS&limit={limit}&offset={offset}&order=-dateSubmitted"        res = wait_get(session, url)        if res is None or res.status_code != 200:            break        try:            res_data = res.json()            if offset == 0:                total_alphas = res_data.get('count', 100)            alphas = res_data.get("results", [])            fetched_alphas.extend(alphas)            if len(alphas) < limit or get_first:                break            offset += limit        except Exception as e:            print(f"解析OS Alpha数据失败: {e}")            break    return fetched_alphas[:total_alphas]def download_data(session, data_path: Path, flag_increment: bool = True) -> Tuple[    Optional[Dict], Optional[pd.DataFrame]]:    """下载并保存基础数据"""    os_alpha_ids = None    os_alpha_pnls = None    exist_alpha = []    if flag_increment:        try:            os_alpha_ids = load_obj(str(data_path / 'os_alpha_ids'))            os_alpha_pnls = load_obj(str(data_path / 'os_alpha_pnls'))            if os_alpha_ids is not None:                exist_alpha = [alpha for ids in os_alpha_ids.values() for alpha in ids]        except Exception as e:            logging.error(f"加载现有数据失败: {e}")    if os_alpha_ids is None:        alphas = get_os_alphas(session, limit=100, get_first=False)    else:        alphas = get_os_alphas(session, limit=30, get_first=True)    alphas = [item for item in alphas if item['id'] not in exist_alpha]    os_alpha_ids, os_alpha_pnls = get_alpha_pnls(session, alphas,                                                 alpha_pnls=os_alpha_pnls,                                                 alpha_ids=os_alpha_ids)    if os_alpha_ids:        save_obj(os_alpha_ids, str(data_path / 'os_alpha_ids'))    if os_alpha_pnls is not None and not os_alpha_pnls.empty:        save_obj(os_alpha_pnls, str(data_path / 'os_alpha_pnls'))    print(        f'新下载的alpha数量: {len(alphas)}, 目前总共alpha数量: {os_alpha_pnls.shape[1] if os_alpha_pnls is not None else 0}')    return os_alpha_ids, os_alpha_pnlsdef load_data(session, data_path: Path) -> Tuple[Optional[Dict], Optional[pd.DataFrame]]:    """加载基础数据"""    try:        os_alpha_ids = load_obj(str(data_path / 'os_alpha_ids'))        os_alpha_pnls = load_obj(str(data_path / 'os_alpha_pnls'))    except Exception as e:        logging.error(f"加载数据失败: {e}")        return None, None    if os_alpha_ids is None or os_alpha_pnls is None:        return None, None    exist_alpha = [alpha for ids in os_alpha_ids.values() for alpha in ids]    os_alpha_pnls = os_alpha_pnls[exist_alpha]    # 计算收益率    os_alpha_rets = os_alpha_pnls - os_alpha_pnls.ffill().shift(1)    # 使用最近4年数据    if not os_alpha_rets.empty:        cutoff_date = pd.to_datetime(os_alpha_rets.index).max() - pd.DateOffset(years=4)        os_alpha_rets = os_alpha_rets[pd.to_datetime(os_alpha_rets.index) > cutoff_date]    return os_alpha_ids, os_alpha_retsdef calc_self_corr(session, alpha_id: str, os_alpha_rets: pd.DataFrame,                   os_alpha_ids: Dict[str, List[str]], alpha_result: Optional[Dict] = None) -> float:    """计算自相关性（返回相关系数值）"""    if alpha_result is None:        alpha_result_res = wait_get(session, f"https://api.worldquantbrain.com/alphas/{alpha_id}")        if alpha_result_res is None or alpha_result_res.status_code != 200:            return 1.0  # 默认高相关性        try:            alpha_result = alpha_result_res.json()        except Exception as e:            print(f"解析Alpha信息失败: {e}")            return 1.0    # 获取目标alpha的PnL数据    alpha_pnl = _get_alpha_pnl(session, alpha_id)    if alpha_pnl.empty:        return 1.0    try:        alpha_pnl = alpha_pnl.set_index('Date')[alpha_id]        alpha_rets = alpha_pnl - alpha_pnl.ffill().shift(1)        # 确定时间范围        if alpha_result.get("type") == "SUPER":            cutoff_date = pd.to_datetime(alpha_rets.index).max() - pd.DateOffset(years=4)        else:            cutoff_date = pd.to_datetime(alpha_rets.index).max() - pd.DateOffset(years=4)        # 过滤数据        if not alpha_rets.empty:            alpha_rets = alpha_rets[pd.to_datetime(alpha_rets.index) > cutoff_date]        # 获取相同区域的OS alpha        region = alpha_result['settings']['region']        region_os_alphas = os_alpha_ids.get(region, [])        if not region_os_alphas:            print(f"区域 {region} 没有可用的OS alpha")            return 1.0        # 确保region_os_alphas中的alpha在os_alpha_rets中存在        available_alphas = [a for a in region_os_alphas if a in os_alpha_rets.columns]        if not available_alphas:            print(f"区域 {region} 没有匹配的OS alpha数据")            return 1.0        # 计算相关性        corr_values = os_alpha_rets[available_alphas].corrwith(alpha_rets)        if corr_values.empty:            return 1.0        max_corr = corr_values.max()        if np.isnan(max_corr):            return 1.0        return max_corr    except Exception as e:        print(f"计算自相关失败: {e}")        traceback.print_exc()  # 打印详细错误信息        return 1.0def calculate_self_correlation(session, data_path: Path, alpha_df: pd.DataFrame) -> pd.DataFrame:    """为Alpha列表计算自相关性"""    print("\n下载自相关性计算所需的基础数据...")    download_data(session, data_path, flag_increment=True)    print("\n加载自相关性计算数据...")    os_alpha_ids, os_alpha_rets = load_data(session, data_path)    if os_alpha_ids is None or os_alpha_rets is None or os_alpha_rets.empty:        print("无法加载基础数据，跳过自相关计算")        alpha_df['self_correlation'] = 1.0  # 默认高相关性        return alpha_df    print(f"\n为 {len(alpha_df)} 个SuperAlpha计算自相关性...")    self_corr_results = []    def process_alpha(row):        alpha_id = row['alpha_id']        try:            corr_value = calc_self_corr(                session=session,                alpha_id=alpha_id,                os_alpha_rets=os_alpha_rets,                os_alpha_ids=os_alpha_ids            )            return {                "alpha_id": alpha_id,                "self_correlation": corr_value            }        except Exception as e:            print(f"计算Alpha {alpha_id} 自相关性失败: {e}")            return {                "alpha_id": alpha_id,                "self_correlation": 1.0  # 默认高相关性            }    # 使用线程池并行处理    with ThreadPoolExecutor(max_workers=MAX_WORKERS_SELF_CORR) as executor:        futures = [executor.submit(process_alpha, row) for _, row in alpha_df.iterrows()]        for future in tqdm(as_completed(futures), total=len(futures), desc="计算自相关性"):            self_corr_results.append(future.result())    # 创建结果DataFrame    self_corr_df = pd.DataFrame(self_corr_results)    # 合并到原始DataFrame    result_df = alpha_df.merge(self_corr_df, on='alpha_id', how='left')    # 标记低自相关Alpha（我们需要的）    result_df['low_self_corr'] = result_df['self_correlation'] < SELF_CORR_THRESHOLD    return result_dfdef set_alpha_properties(session, alpha_id: str, name: Optional[str] = None, color: Optional[str] = None) -> None:    """设置Alpha属性（名称、颜色）"""    params = {}    if name:        params["name"] = name    if color:        params["color"] = color    if not params:        return    try:        response = session.patch(f"https://api.worldquantbrain.com/alphas/{alpha_id}", json=params)        if response.status_code == 200:            print(f"成功更新Alpha {alpha_id} 属性")        else:            print(f"更新Alpha属性失败: {response.status_code} - {response.text}")    except Exception as e:        print(f"更新Alpha属性异常: {e}")# ===== 主函数 =====def main():    # 配置参数    class cfg:        username = ""        password = ""        data_path = Path('.')    # 登录    print("登录WorldQuant Brain...")    session = sign_in(cfg.username, cfg.password)    if not session:        print("登录失败，请检查用户名和密码")        return    # 第一步：获取符合条件的SuperAlpha    print(f"\n获取符合条件的SuperAlpha ({START_DATE} 到 {END_DATE}, {REGION})...")    alpha_data = get_submit_alphas(        session=session,        start_date=START_DATE,        end_date=END_DATE,        sharpe_th=SHARPE_THRESHOLD,        fitness_th=FITNESS_THRESHOLD,        region=REGION,        alpha_num=ALPHA_NUM    )    if not alpha_data:        print("没有找到符合条件的SuperAlpha")        return    alpha_df = pd.DataFrame(alpha_data)    # 第二步：计算自相关性    result_df = calculate_self_correlation(        session=session,        data_path=cfg.data_path,        alpha_df=alpha_df    )    # 第三步：优先处理低相关性Alpha    low_corr_df = result_df[result_df['low_self_corr']]    low_corr_count = len(low_corr_df)    print(f"\n发现 {low_corr_count} 个低自相关Alpha (<{SELF_CORR_THRESHOLD})")    # 标记低相关性Alpha（可选）    for _, row in low_corr_df.iterrows():        alpha_id = row['alpha_id']        corr_value = row['self_correlation']        new_name = f"LOW_CORR_{corr_value:.4f}"        set_alpha_properties(session, alpha_id, name=new_name, color="GREEN")    # 第四步：保存结果到Excel    # 按自相关性升序排序（低相关性在前）    result_df = result_df.sort_values(by='self_correlation', ascending=True)    # 添加排名    result_df['rank'] = result_df['self_correlation'].rank(method='min')    output_columns = [        "rank", "alpha_id", "alpha_type", "check_status", "sharpe", "turnover",        "fitness", "margin", "dateCreated", "longCount", "shortCount", "decay",        "neutralization_name", "self_correlation", "low_self_corr"    ]    # 确保所有列都存在    available_columns = [col for col in output_columns if col in result_df.columns]    result_df = result_df[available_columns]    # 保存到Excel    with pd.ExcelWriter(OUTPUT_FILE) as writer:        result_df.to_excel(writer, sheet_name='Low Correlation SuperAlphas', index=False)        print(f"\n结果已保存到: {OUTPUT_FILE}")    # 打印统计信息    print("\n统计信息:")    print(f"SuperAlpha总数: {len(result_df)}")    print(f"低自相关Alpha数量: {low_corr_count} (<{SELF_CORR_THRESHOLD})")    if 'self_correlation' in result_df.columns:        print(f"平均自相关性: {result_df['self_correlation'].mean():.4f}")        print(f"最低自相关性: {result_df['self_correlation'].min():.4f}")        print(f"最高自相关性: {result_df['self_correlation'].max():.4f}")    # 中性化设置分布统计    if 'neutralization_name' in result_df.columns:        print("\n中性化设置分布:")        print(result_df['neutralization_name'].value_counts())    # Alpha类型分布    if 'alpha_type' in result_df.columns:        print("\nAlpha类型分布:")        print(result_df['alpha_type'].value_counts())    # 打印前10个低相关性Alpha    if low_corr_count > 0:        print("\n前10个低相关性SuperAlpha:")        print(low_corr_df.head(10)[['alpha_id', 'self_correlation', 'sharpe', 'fitness']].to_string(index=False))    else:        print("\n未发现低相关性SuperAlpha")if __name__ == "__main__":    main()
```

---

## 讨论与评论 (5)

### 评论 #1 (作者: XW23690, 时间: 4个月前)

赞。运行效率很快

---------------------------------------------------------------------------
α≠运气 |α=Edge E [PnL]=∑(E [r_i]×w_i) σ↓→Sharpe↑
Factor=Signal-Noise Backtest→Live→Repeat βNeutral|αMax
Win>Loss|Risk<Reward InSample→OutOfSample
---------------------------------------------------------------------------

---

### 评论 #2 (作者: LK39823, 时间: 3个月前)

感谢大佬的分享，完美

====================================================================================

这个代码用起来一天能测多少pc？

====================================================================================

---

### 评论 #3 (作者: LX57490, 时间: 3个月前)

@ [LK39823](/hc/zh-cn/profiles/28842771803415-LK39823)

这是计算SC的帖子，每小时最多计算两千个Super Alpha 的SA，如果想测PC看另外一篇帖子！

[【新人向-SA的Prod检测 —— 24h可检测600个！】即插即用！结合上一篇SA自相关文章！ – WorldQuantBrain-CN]([L2] 【新人向-SA的Prod检测  24h可检测600个】即插即用结合上一篇SA自相关文章_37554208805911.md)

================================================================================

**高筑墙！广积粮！缓称王！**

================================================================================

---

### 评论 #4 (作者: 顾问 SJ65808 (Rank 20), 时间: 3个月前)

哇哦，学到了，很棒的工具

====================================================================================
==================纸上得来终觉浅，绝知此事要躬行======================================

---

### 评论 #5 (作者: CZ39633, 时间: 2个月前)

====================================================================================                        感谢大佬的pc代码分享 ,太有用了，很棒的工具                                                                                  ================================自信人生两百年，会当水击三千里==========================

---

