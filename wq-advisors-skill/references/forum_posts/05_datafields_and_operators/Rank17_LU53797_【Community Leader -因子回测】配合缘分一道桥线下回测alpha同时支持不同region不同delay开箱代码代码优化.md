# 【Community Leader -因子回测】配合缘分一道桥线下回测alpha，同时支持不同region不同delay，开箱代码代码优化

- **链接**: 【Community Leader -因子回测】配合缘分一道桥线下回测alpha同时支持不同region不同delay开箱代码代码优化.md
- **作者**: 顾问 LU53797 (Rank 17)
- **发布时间/热度**: 6个月前, 得票: 10

## 帖子正文

首先由衷感谢开发者大佬打造的 “缘分一道桥” 工具，它的实用性非常突出！在使用过程中，我发现一个小问题：下载的回测 alpha 数据包含不同的 region 与 delay 维度，这使得原有回测工具无法适配使用。为此，我在原程序的基础上做了小幅修改，使其能够同时支持不同 region 和不同 delay 的场景。修改后的代码如下，大家直接替换 json_pipeline_backtest.py 文件内容即可（温馨提示：操作前请先备份原文件，避免出现意外情况）。#!/usr/bin/env python3# -*- coding: utf-8 -*-import jsonimport timeimport signalimport requestsimport asyncioimport randomfrom datetime import datetimefrom typing import List, Dict, Anyfrom session_manager import initialize_global_session, get_global_session_manager, api_request_with_retrydef login():"""登录WorldQuant Brain API"""username = "账号"password = "密码"s = requests.Session()s.auth = (username, password)response = s.post('https://api.worldquantbrain.com/authentication')print(f"登录响应: {response.status_code}")return sdef create_authenticated_session():"""创建已认证的session - 用于全局session管理器"""return login()class JSONAlphaManager:"""JSON Alpha数据管理器"""def __init__(self, json_file_path: str):self.json_file_path = json_file_pathself.alphas = []self.successful_alphas = 0  # 跟踪成功提交的alpha数量self.load_alphas()def load_alphas(self):"""从JSON文件加载Alpha数据"""try:with open(self.json_file_path, 'r', encoding='utf-8') as f:data = json.load(f)# 支持列表或包含alphas字段的字典格式self.alphas = data if isinstance(data, list) else data.get('alphas', [])print(f"成功加载 {len(self.alphas)} 个Alpha表达式")# 检测RAM参数使用情况ram_count=sum(1foralphainself.alphasif isinstance(alpha, dict) and alpha.get('settings', {}).get('neutralization') == 'RAM')if ram_count > 0:print(f"🔍 检测到 {ram_count} 个使用RAM中性化的Alpha配置")print("💡 注意：保留原始RAM参数值，不进行替换（根据用户反馈，网站支持RAM参数）")except Exception as e:print(f"加载JSON文件失败: {e}")raisedef get_pending_alphas(self) -> List[Dict]:"""获取待处理的Alpha列表"""return self.alphasclass JSONTaskManager:"""JSON任务管理器 - 基于PipelineTaskManager的JSON版本"""def __init__(self, alpha_manager: JSONAlphaManager, task_size=10, max_concurrent_tasks=8):self.alpha_manager = alpha_managerself.task_size = task_sizeself.max_concurrent_tasks = max_concurrent_tasksself.alpha_index = 0self.active_tasks = {}  # {task_id: task_info}self.completed_tasks = []self.task_counter = 0self.pending_alphas = alpha_manager.get_pending_alphas()def has_more_alphas(self) -> bool:"""检查是否还有更多alpha待处理"""return self.alpha_index < len(self.pending_alphas)def get_next_task(self):"""获取下一个任务的alpha列表，确保同一个batch只包含相同region和delay设置的配置"""if not self.has_more_alphas():return None# 获取当前剩余的alpha列表remaining_alphas = self.pending_alphas[self.alpha_index:]# 提取当前alpha的配置first_alpha = remaining_alphas[0]if isinstance(first_alpha, dict) and 'settings' in first_alpha:region = first_alpha['settings'].get('region', 'UNKNOWN')delay = first_alpha['settings'].get('delay', -1)else:region, delay = 'UNKNOWN', -1# 收集相同region和delay的alphabatch_alphas = []for alpha in remaining_alphas[:self.task_size]:if isinstance(alpha, dict) and 'settings' in alpha:alpha_region = alpha['settings'].get('region', 'UNKNOWN')alpha_delay = alpha['settings'].get('delay', -1)if alpha_region == region and alpha_delay == delay:batch_alphas.append(alpha)else:breakelse:# 对于无效配置，单独处理batch_alphas = [alpha]break# 更新索引和计数器self.alpha_index += len(batch_alphas)self.task_counter += 1task_info = {'task_id': self.task_counter,'alphas': batch_alphas,'region': region,'delay': delay,'start_time': None,'progress_url': None,'status': 'pending'}print(f"🔄 创建新任务 #{self.task_counter}: 包含{len(batch_alphas)}个alpha，region={region}，delay={delay}")return task_infodef add_active_task(self, task_info, progress_url):"""添加活跃任务"""task_info.update({'progress_url': progress_url,'status': 'running','start_time': time.time()})self.active_tasks[task_info['task_id']] = task_infodef complete_task(self, task_id, result):"""完成任务"""if task_id not in self.active_tasks:returntask_info = self.active_tasks.pop(task_id)task_info.update({'status': 'completed','end_time': time.time(),'result': result})# 检查是否已经在completed_tasks中（避免重复添加）existing_task = next((t for t in self.completed_tasks if t['task_id'] == task_id), None)if existing_task:existing_task.update({'result': result,'end_time': time.time()})else:self.completed_tasks.append(task_info)def get_active_task_count(self) -> int:"""获取活跃任务数量"""return len(self.active_tasks)def can_submit_new_task(self) -> bool:"""检查是否可以提交新任务"""return self.get_active_task_count() < self.max_concurrent_tasks and self.has_more_alphas()def get_progress_summary(self) -> Dict[str, Any]:"""获取进度摘要，包含当前处理位置的详细信息"""total_alphas = len(self.pending_alphas)processed_alphas = self.alpha_manager.successful_alphasreturn {'total_alphas': total_alphas,'processed_alphas': processed_alphas,'current_position': self.alpha_index,'completed_tasks': len(self.completed_tasks),'active_tasks': self.get_active_task_count(),'progress_percentage': (processed_alphas / total_alphas * 100) if total_alphas > 0 else 0}def submit_alpha_batch(session: requests.Session, alphas: List[Dict]) -> str:"""提交Alpha批次到API - 使用全局session管理器根据API要求，单个模拟直接提交对象，多个模拟提交数组"""try:session_manager = get_global_session_manager()# 根据alpha数量决定提交格式payload = alphas[0] if len(alphas) == 1 else alphasprint(f"📤 {'提交单个Alpha模拟' if len(alphas) == 1 else f'提交批量Alpha模拟，共{len(alphas)}个'}")response = session_manager.post('https://api.worldquantbrain.com/simulations', json=payload)if response.status_code == 201:location = response.headers.get('Location')if location:return locationraise Exception("API响应中缺少Location头")raise Exception(f"API返回错误状态码: {response.status_code}, 响应: {response.text}")except Exception as e:raise Exception(f"提交Alpha批次失败: {e}")def check_task_status(session: requests.Session, progress_url: str) -> Dict[str, Any]:"""检查任务状态 - 使用全局session管理器"""try:session_manager = get_global_session_manager()response = session_manager.get(progress_url, timeout=30)if response.status_code != 200:return {'status': 'error', 'error': f'HTTP {response.status_code}'}if not response.content.strip():return {'status': 'error', 'error': 'Empty response'}try:result = response.json()status = result.get('status', 'UNKNOWN')if status in ['DONE', 'COMPLETE']:return {'status': 'completed', 'result': result}elif status in ['FAILED', 'ERROR']:return {'status': 'failed', 'result': result}return {'status': 'running', 'result': result}except ValueError as e:return {'status': 'error', 'error': f'JSON解析失败: {e}'}except Exception as e:error_type = 'Request timeout' if isinstance(e, requests.exceptions.Timeout) else \'Connection error' if isinstance(e, requests.exceptions.ConnectionError) else str(e)return {'status': 'error', 'error': error_type}async def _submit_task_with_retry(task_info, session_manager, alpha_manager, max_retries=3, task_type="任务") -> tuple:"""提交任务并处理重试逻辑"""for retry_count in range(max_retries):try:# 直接使用alphas，移除不必要的prepare_alpha_batch函数调用progress_url = submit_alpha_batch(session_manager, task_info['alphas'])# 更新成功计数alpha_manager.successful_alphas += len(task_info['alphas'])# 输出基本提交信息processed_alphas = alpha_manager.successful_alphasprint(f"✅ 提交{task_type} {task_info['task_id']}: {len(task_info['alphas'])}个alpha")print(f"   📊 累计已提交: {processed_alphas}个alpha")return True, progress_urlexcept Exception as e:print(f"❌ 提交{task_type} {task_info['task_id']}失败 (第{retry_count + 1}次): {e}")if retry_count < max_retries - 1:print(f"   ⏳ 等待15秒后重试...")await asyncio.sleep(15)else:print(f"🛑 {task_type} {task_info['task_id']}提交失败，已达到最大重试次数")return False, Noneasync def json_pipeline_simulate(task_manager: JSONTaskManager, initial_task_count: int = None) -> str:"""JSON流水线模式的多任务模拟器Args:task_manager: JSONTaskManager实例，管理任务队列和状态initial_task_count: 初始提交的任务数量，如果为None则使用默认值Returns:str: 执行状态"""# 设置中断处理标志interrupted = Falsedef signal_handler(signum, frame):nonlocal interruptedprint("\n⚠️  检测到中断信号，正在保存当前状态...")interrupted = True# 注册信号处理器signal.signal(signal.SIGINT, signal_handler)signal.signal(signal.SIGTERM, signal_handler)# 初始化全局session管理器initialize_global_session(create_authenticated_session)s = get_global_session_manager().get_session()print(f"开始JSON流水线模拟，总共 {len(task_manager.pending_alphas)} 个alpha")# 初始化失败任务列表failed_tasks = []# 初始化：根据参数决定初始提交的任务数量initial_submit_count = min(initial_task_count or task_manager.max_concurrent_tasks, task_manager.max_concurrent_tasks)# 提交初始任务for _ in range(initial_submit_count):if interrupted or not task_manager.can_submit_new_task():breaktask_info = task_manager.get_next_task()if task_info:success, progress_url = await _submit_task_with_retry(task_info, s, task_manager.alpha_manager)if success:task_manager.add_active_task(task_info, progress_url)else:failed_tasks.append(task_info)print(f"📋 初始化完成，当前活跃任务数: {task_manager.get_active_task_count()}")# 初始提交后，尝试填补失败任务的空槽while not interrupted and task_manager.get_active_task_count() < task_manager.max_concurrent_tasks and task_manager.has_more_alphas():replacement_task = task_manager.get_next_task()if replacement_task:success, progress_url = await _submit_task_with_retry(replacement_task, s, task_manager.alpha_manager, task_type="替补任务")if success:task_manager.add_active_task(replacement_task, progress_url)else:failed_tasks.append(replacement_task)else:break# 主循环：监控任务完成并提交新任务while (task_manager.get_active_task_count() > 0) or (task_manager.has_more_alphas() and not interrupted):# 检查是否所有Alpha都已提交完成total_alphas = len(task_manager.pending_alphas)if task_manager.alpha_manager.successful_alphas >= total_alphas and task_manager.get_active_task_count() > 0:print(f"✅ 所有Alpha已提交完成 ({task_manager.alpha_manager.successful_alphas}/{total_alphas})，但仍有 {task_manager.get_active_task_count()} 个任务在执行")print(f"🚀 返回 'all_submitted' 状态，允许启动下一个文件")return 'all_submitted'# 检查中断信号if interrupted:print("🛑 收到中断信号，停止提交新任务，等待现有任务完成...")if task_manager.get_active_task_count() == 0:print("🛑 所有活跃任务已完成，正在保存当前状态并退出...")return 'interrupted'completed_task_ids = []# 检查活跃任务状态，但减少查询频率api_error_count = 0tasks_to_check = list(task_manager.active_tasks.items())# 如果任务数超过5个，每次只检查一半的任务if len(tasks_to_check) > 5:tasks_to_check = random.sample(tasks_to_check, len(tasks_to_check) // 2)for task_id, task_info in tasks_to_check:try:progress_url = task_info['progress_url']# API请求逻辑，最多重试2次status_result = Nonefor retry_count in range(2):try:status_result = check_task_status(s, progress_url)if status_result['status'] != 'error':breakexcept Exception as e:status_result = {'status': 'error', 'error': str(e)}# 仅在最后一次重试失败时才输出错误信息if retry_count == 1:print(f"任务 {task_id} 状态查询失败: {status_result.get('error', 'Unknown error')}")if not status_result or status_result['status'] == 'error':api_error_count += 1continue# 处理任务完成或失败if status_result['status'] in ['completed', 'failed']:task_manager.complete_task(task_id, status_result['result'])completed_task_ids.append(task_id)if status_result['status'] == 'failed':print(f"ℹ️  任务 {task_id} 状态为'failed'，但Alpha已成功提交至系统")failed_tasks.append(task_info)else:print(f"任务 {task_id} 完成")# 立即提交新任务填补空槽if not interrupted and task_manager.can_submit_new_task():new_task_info = task_manager.get_next_task()if new_task_info:task_type = "替换任务" if status_result['status'] == 'failed' else "新任务"success, new_progress_url = await _submit_task_with_retry(new_task_info, s, task_manager.alpha_manager, task_type=task_type)if success:task_manager.add_active_task(new_task_info, new_progress_url)else:failed_tasks.append(new_task_info)except Exception as e:print(f"检查任务 {task_id} 状态时出错: {e}")api_error_count += 1if api_error_count >= 5:print("检测到大量API错误，休息60秒后继续...")await asyncio.sleep(60)api_error_count = 0# 只在有任务完成时才显示进度更新if completed_task_ids:progress = task_manager.get_progress_summary()current_time = datetime.now().strftime("%H:%M:%S")print(f"[{current_time}] 📈 任务完成进度更新:")print(f"  ✅ 已处理: {progress['processed_alphas']}/{progress['total_alphas']} ({progress['progress_percentage']:.1f}%)")print(f"  ⏳ 当前位置: 正在处理第{progress['current_position'] + 1}个Alpha")print(f"  🔄 任务状态: {progress['active_tasks']}活跃/{progress['completed_tasks']}完成")# 轮询间隔，减少查询频率await asyncio.sleep(120)print("所有任务完成")final_progress = task_manager.get_progress_summary()print(f"最终统计: 处理了 {final_progress['processed_alphas']} 个alpha，完成了 {final_progress['completed_tasks']} 个任务")if failed_tasks:print(f"失败任务数量: {len(failed_tasks)}")return 'completed'def run_json_pipeline_backtest(json_file_path: str, task_size: int = 10, max_concurrent_tasks: int = 8,initial_task_count: int = None) -> str:"""运行JSON流水线回测Args:json_file_path: JSON文件路径task_size: 每个任务包含的alpha数量max_concurrent_tasks: 最大并发任务数initial_task_count: 初始提交的任务数量"""try:# 加载JSON数据并创建任务管理器alpha_manager = JSONAlphaManager(json_file_path)task_manager = JSONTaskManager(alpha_manager, task_size, max_concurrent_tasks)print(f"开始JSON流水线回测")print(f"总Alpha数量: {len(task_manager.pending_alphas)}")print(f"任务大小: {task_size}")print(f"最大并发任务数: {max_concurrent_tasks}")# 运行流水线模拟result = asyncio.run(json_pipeline_simulate(task_manager, initial_task_count))print(f"\n回测完成，结果: {result}")return resultexcept Exception as e:print(f"运行JSON流水线回测失败: {e}")raise

---

## 讨论与评论 (3)

### 评论 #1 (作者: ZY23886, 时间: 6个月前)

替换 json_pipeline_backtest.py  这个文件我怎么没找到啊 大佬

---

### 评论 #2 (作者: DY13559, 时间: 6个月前)

学习了，谢谢大佬

---

### 评论 #3 (作者: YQ84572, 时间: 6个月前)

大佬的分享与贡献，非常使用的工具，在使用缘分一道桥的时候确实会有这个问题，回测起来比较麻烦需要一个个调设置，这个工具直接方便了缘分的使用，赞！================================================================================评论区 ================================================================================

---

